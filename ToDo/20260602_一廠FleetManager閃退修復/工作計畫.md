# 工作計畫 — 一廠 FleetManager 閃退修復（自二廠移植）

> 建立日期：2026-06-02
> 來源：二廠 SecondFactory commit a316d00..f6d342a（三層防護 + 全檔參數化）
> 詳細差異比對與 14 個 SQL 點清單見 `plan.md`；驗收見 `spec.md`
> de-risk 已完成：一廠 SAA_PUBLIC.LogType = {Normal,Warnning,Error}（與二廠同，可編譯）；SqlHelper.cs = Big5（需轉碼）

## 執行順序（提交順序）

先建底層能力 → 疊防護 → 補測試。轉碼必為獨立 commit。
**鐵則：一廠既有派送差異（L 區歸系統群、停用 BtoA、無空板回收）一律保留，只加防護外殼，不換成二廠邏輯。**
**只 stage 本案檔案；不碰既有未 commit 變更（ACC/.../Dispatch.cs、FHtSetting.config）。**

## 單一任務完成 4 道 Gate

每個子任務完成、標記 `[x]` 之前必須全部通過：

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | 對應專案建置 | 0 error（warning 允許） |
| 2. Test | TDD: 對應測試 / 一般: 回歸測試 | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 對應驗收標準 | 全部滿足 |
| 4. Regression | 若屬重構 / 效能優化 | 完整測試套件 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者。

## 跨優先順序驗證點

### 🚦 驗證點 #1：P0 → P1（進入第一個 P1 前）
完成 P0（C1-C2 底層）後：build 0 error、既有 cPairTests 通過 → 才進 P1。

### 🚦 驗證點 #2：P1 → P2（進入第一個 P2 前）
完成 P1（C3-C6 修復）後：跑全部測試 0 失敗、Layer A/B 綠燈、`git diff` 確認一廠派送差異未動 → 才進 P2。

## 工作項目

### P0 — 底層能力
- [-] [P0] C1 `SqlHelper.cs` 整檔 Big5→UTF-8 BOM 轉碼（0 邏輯，**獨立 commit**）
- [ ] [P0] C2 `SqlHelper.cs` 新增 Query/WriteSqlByAutoOpen(string, params SqlParameter[]) 多載 + AddParameters(clone)

### P1 — 修復
- [ ] [P1] C3 `cPair.cs` 全檔 SQL 參數化（plan.md 14 點；Block IN-list 保留）+ SP() 工廠 + using SqlClient
- [ ] [P1] 🧪 C4 `cPair.cs` GenerateoRequireByoNeed 單筆 try/catch 隔離（抽 ProcessSingleoNeed/HandleoNeedRowException/IsDataException/QuarantineoNeedByTaskDateTime/SafeCol；保留一廠 switch）+ 05 補印
- [ ] [P1] C5 `cPair.cs` MainProcess while 外層 try/catch 保命
- [ ] [P1] C6 `cTest/Program.cs` 全域攔截（AppDomain.UnhandledException + Application.ThreadException + WriteCrashLog + CrashTrace）

### P2 — 測試與驗收
- [ ] [P2] 🧪 C7 新建測試：IntegrationTestBase（防呆改 DB 名白名單）+ ResilienceTests + ParameterizationTests + csproj Compile 項
- [ ] [P2] C8 全套測試綠燈 + acceptance-checker 核對 spec.md

## Commit 策略
- 格式：`refactor/feat/fix/test(dispatch|sqlhelper|host): <說明>`
- C1 轉碼獨立 commit（0 邏輯）。
- 每 commit 後跑 industrial-test-runner（net4.8：msbuild + vstest）確認 build。
- 版本號變更需暫停取得確認。

## 🧪 測試案例矩陣（同二廠 Layer A/B/C）
- Layer B：毒筆(觸發資料類例外) + 正常筆 → 不拋/隔離X/好筆續成。
- Layer A：O'Brien / 注入字串(非破壞性) / RackId 單引號 → 原值 round-trip。
- Layer C：正常筆 → oRequire/oNeed=Y/oPort BgnToEnd。
