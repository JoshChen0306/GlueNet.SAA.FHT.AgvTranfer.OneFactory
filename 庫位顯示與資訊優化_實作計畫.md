# 庫位顯示與資訊優化 實作計畫

本計畫紀錄了 Dispatch 派送地圖上庫位資訊顯示的優化過程，包含標籤增強與最終的簡化調整。

## 變更紀錄

### 階段一：庫位編號標籤增強 (2025-12)
為了讓使用者在操作時能快速識別站點，在地圖圖示下方增加了 `station-label`。
- **功能**：預設隱藏，僅在滑鼠懸停（Hover）時顯示站點縮寫（如 A1, K2）。
- **實作方式**：在 `_MapPartial.cshtml` 增加 `<span>` 元素，並於 `site.css` 定義淡入效果。

### 階段二：移除文字標籤以簡化 UI (最終狀態)
根據使用者回饋，雖然標籤有助於識別，但在站點密集或縮放比例較小時會造成畫面雜亂。
- **調整原因**：追求極致清爽的 UI，並避免標籤重疊。
- **最終方案**：移除地圖上的文字標籤元素，統一由 **Tooltip (懸停提示)** 承載所有詳細資訊。
- **保留功能**：滑鼠移至圖示上仍會顯示站點編號、狀態、RackId 及工單。

---

## 最終實作細節 (Proposed Changes)

### [WebGui/SCP]

#### [MODIFY] [_MapPartial.cshtml](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/Views/Shared/_MapPartial.cshtml)
移除標籤渲染，僅保留容器結構以維持版面穩定。

#### [MODIFY] [site.css](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/wwwroot/css/site.css)
清理已廢棄的標籤樣式，節約資源。

#### [MODIFY] [Dispatch.js](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/wwwroot/js/Dispatch.js)
確保地圖重新載入後，Tooltip 初始化邏輯運作正常（已於 `loadMapDataLocal` 中實作）。

---

## 驗證計畫

### 手動驗證
1. **地圖畫面**：確認 1F~4F 各樓層地圖中，庫位下方皆不再出現文字標籤。
2. **懸停提示**：滑鼠停在庫位上，確認會彈出浮動視窗，正確顯示「站點: XXX」及其他狀態資訊。
3. **性能檢查**：確認移除 CSS 後，地圖縮放或切換時依然穩定。
