# 工作計畫 — 修正 SCP 儲架設定頁顯示二廠地圖

## 任務概要

- **專案**：FHT-AGV-OneFactory（高技一廠）/ `WebGui/SCP`
- **類型**：Bug 修復 + 小型重構
- **分析力度**：標準模式
- **TDD 任務數**：0（全部為字串修正 / UI 模板 / 邏輯複製，不適合 TDD）

---

## 子任務拆解

### Commit 1 — 字串修正（熱修等級）

- [x] [P0] 修正 `WebGui/SCP/Controllers/PortController.cs:38` 註解 `FHT2-1F` → `FHT1-1F`
- [x] [P0] 修正 `WebGui/SCP/Controllers/PortController.cs:39` `string area = $"FHT1-{floor}"`
- [x] [P0] 修正 `WebGui/SCP/Controllers/PortController.cs:67` `ViewBag.MapImage = $"/img/FHT1-{floor}.png"`
- [x] [P0] 修正 `WebGui/SCP/Views/Shared/_MapPartial.cshtml:31` `"FHT2-1F"` → `"FHT1-1F"`
- [x] [P0] 修正 `WebGui/SCP/wwwroot/js/Dispatch.js:1139` 降級預設值 `'FHT2-1F'` → `'FHT1-1F'`
- [x] [P0] **Sweep Gate**：`grep -r "FHT2" WebGui/SCP` 結果為 0 行
- [x] [P0] **Build Gate**：`SCP.csproj` build 0 error
- [x] [P0] Commit：`fix: 修正 SCP 儲架設定頁顯示二廠地圖（FHT2→FHT1）`

### Commit 2 — 動態頁籤改造

- [x] [P1] `PortController` 加入 `GetUserAllowedFloors()`（從 `CommonController.cs:75-L130` 左右複製），加 `// TODO: 未來抽成共用 FloorService` 註解
- [x] [P1] `PortController.Index` 讀取 `AgvSetting` + `FloorSettings`，設定 `ViewBag.AllowedFloors` 與 `ViewBag.FloorDisplayNames`
- [x] [P1] `floor` 參數容錯：若 query 傳入的 `floor` 對應 `FHT1-{floor}` 不在 `AllowedFloors` 內，改用 `AllowedFloors` 第一個樓層（比照 `CommonController.ShowMap:L35-38`）
- [x] [P1] 改造 `Views/Port/Index.cshtml:L9-L20` 樓層頁籤改用 `ViewBag.AllowedFloors` + `ViewBag.FloorDisplayNames` 動態產生
- [x] [P1] **Build Gate**：`SCP.csproj` build 0 error
- [x] [P1] **Acceptance Gate**：啟動 SCP 實際驗證
  - 儲架設定頁只顯示「1F」「3F」兩個頁籤
  - 1F 頁籤：地圖為 FHT1-1F.png、充電站圖示顯示正確
  - 3F 頁籤：地圖為 FHT1-3F.png
  - 站點位置正確（依 `appsettings.AgvSetting` 座標轉換）
- [x] [P1] Commit：`refactor: Port 頁籤改用 AllowedFloors 動態產生`

---

## TDD 判斷總表

| 子任務 | TDD | 理由 |
|--------|:---:|------|
| Commit 1 全部 | ❌ | 純字串修正，測試收益 < 維護成本 |
| `GetUserAllowedFloors` 複製 | ❌ | 邏輯從 `CommonController` 原封複製、原本就無測試；補測應整個抽 `FloorService` 一併處理（非本次範圍） |
| `PortController.Index` 組 ViewBag | ❌ | Controller Integration Test 需 mock IConfiguration + DbContext，成本過高 |
| `Port/Index.cshtml` 頁籤改造 | ❌ | 純 UI 模板與資料繫結（排除條件 1） |

**結論**：全部走一般任務 Build + 手動驗證流程。

---

## 完成前 4 道 Gate 驗證

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | `msbuild WebGui/SCP/SCP.csproj` | 0 error |
| 2. Test | 現有 SCP 測試（若有） | 全部通過 |
| 3. Acceptance | 逐項核對 `spec.md` 驗收標準（8 項） | 全數滿足 |
| 4. Sweep | `grep -r "FHT2" WebGui/SCP` | 0 行 |

---

## Commit 策略

拆 2 個 commit：

1. **Commit 1（熱修）**：僅字串修正（5 處）— 客戶若需熱修只挑這個 commit 即可出版
2. **Commit 2（改善）**：頁籤動態化 — 屬改善性質，隨後續版本發佈

Commit 順序：先 1 後 2，不得合併。
