# 工作計畫 — SCP 即時更新 Service 硬化

> 建立日期：2026-06-05
> 來源：客戶 SCP 即時更新需 F5 問題排查；code-reviewer 對 `Service.cs` 判定 FAIL（3 Critical + 2 Warning）
> 前置 commit：`a66a14c`（Hub.js 自動重連 + Service.cs Start/Register 防護）
> 驗收標準見 `spec.md`

## 範圍鐵則

- 只動 `WebGui/SCP/Services/Service.cs`，純健壯性修補、0 商業邏輯變更。
- 6 條通知查詢語意與 6 個推播事件名稱**一律不變**（前端 `connection.on` 依賴）。
- 不碰客戶端環境設定（Service Broker / 帳號權限）；不碰既有未 commit 的 `FHtSetting.config`。
- 動手前再次確認 `Service.cs` 為 UTF-8 BOM，禁止局部混編。

## 單一任務完成 4 道 Gate

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | SCP.csproj 建置 | 0 error（warning 允許） |
| 2. Test | 回歸測試（若有） | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 驗收標準 | 對應項目全部滿足 |
| 4. Regression | 屬健壯性重構 → 確認 6 推播事件名稱未變 + 無新增空 catch | 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者。

## 工作項目

### P1 — 穩定性（災難級風險優先）
- [ ] [P1] T1 6 個 `OnChange` 加 `e.Type == SqlNotificationType.Change` 守衛；非 Change 記 Warning 且不重註冊（擋無限重註冊迴圈，Critical #3）
- [ ] [P1] T2 移除無界成長且從未讀取的 `_dependency` List 洩漏點；若保留則改執行緒安全 + 重註冊前 `OnChange -=` 解除舊訂閱（Critical #1+#2）
- [ ] [P1] T3 每個 `async void OnChange` body 包 try/catch + Log，避免 `SendAsync` 例外打掛行程（Warning #1）

### P2 — 收尾防護
- [ ] [P2] T4 `StopAsync` 的 `Stop()` 補 try/catch + Log（與 Start 對稱）（Warning #2）
- [ ] [P2] T5 `_connectionString` null/empty 檢查，log 明確區分「連線字串缺失」與「broker 未啟用」（Warning #3）

## 跨優先順序驗證點

### 🚦 P1 → P2
完成 T1~T3 後：SCP build 0 error、`git diff` 確認 6 推播事件名稱與 6 查詢語意未變、無新增空 catch → 才進 P2。

## Commit 策略
- 格式：`fix(scp): <說明>` 或 `refactor(scp): <說明>`
- T1~T3（穩定性）可一個 commit；T4~T5 一個 commit。
- 每 commit 後跑 industrial-test-runner（net6.0：dotnet build/test）確認 build。
- 只 stage 本案 `Service.cs`，不碰其他未 commit 變更。
