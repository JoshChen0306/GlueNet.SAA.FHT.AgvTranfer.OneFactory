# 儲架設定頁面多樓層支援

## 需求說明

在儲架設定 (Port) 頁面新增樓層切換功能，支援 1F、2F、3F、4F。

## 現況分析

| 項目 | 現況 |
|------|------|
| 地圖 | 固定使用 `/img/Map.png` |
| 站點 | 讀取所有 oPort，無樓層過濾 |
| 樓層切換 | ❌ 無 |

---

## Proposed Changes

### 後端 Controller

#### [MODIFY] [PortController.cs](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/Controllers/PortController.cs)

1. 新增樓層參數
2. 依樓層過濾 oPort
3. 回傳對應樓層的地圖圖片路徑

```csharp
public IActionResult Index(string floor = "1F")
{
    // 樓層與區域對應
    var floorBlocks = new Dictionary<string, string[]>
    {
        { "1F", new[] { "A", "B", "C", "D", "E", "F", "EE" } },
        { "2F", new[] { "H" } },
        { "3F", new[] { "J" } },
        { "4F", new[] { "K", "L" } }
    };
    
    var blocks = floorBlocks.ContainsKey(floor) ? floorBlocks[floor] : floorBlocks["1F"];
    
    // 過濾該樓層的站點
    List<oPort> query = _DBContext.oPort
        .Where(p => blocks.Contains(p.Block))
        .ToList();
    
    ViewBag.CurrentFloor = floor;
    ViewBag.MapImage = $"/img/FHT2-{floor}.png";
}
```

---

### 前端 View

#### [MODIFY] [Index.cshtml](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/Views/Port/Index.cshtml)

1. 新增樓層切換按鈕
2. 動態顯示地圖圖片

```html
<!-- 樓層切換按鈕 -->
<div class="btn-group mb-3">
    <a href="/Port?floor=1F" class="btn btn-outline-primary @(ViewBag.CurrentFloor == "1F" ? "active" : "")">1F</a>
    <a href="/Port?floor=2F" class="btn btn-outline-primary @(ViewBag.CurrentFloor == "2F" ? "active" : "")">2F</a>
    <a href="/Port?floor=3F" class="btn btn-outline-primary @(ViewBag.CurrentFloor == "3F" ? "active" : "")">3F</a>
    <a href="/Port?floor=4F" class="btn btn-outline-primary @(ViewBag.CurrentFloor == "4F" ? "active" : "")">4F</a>
</div>

<!-- 動態地圖 -->
<img src="@ViewBag.MapImage" height="500" />
```

---

## 地圖檔案

| 樓層 | 地圖檔案 | 狀態 |
|------|----------|------|
| 1F | `/img/FHT2-1F.png` | ✅ 已存在 |
| 2F | `/img/FHT2-2F.png` | ✅ 已存在 |
| 3F | `/img/FHT2-3F.png` | ✅ 已存在 |
| 4F | `/img/FHT2-4F.png` | ✅ 已存在 |

---

## oPort 站點座標

> [!IMPORTANT]
> 每個樓層的站點需要在 `oPort.Remark` 欄位設定座標 (`X,Y,角度`)，才能正確顯示在地圖上。

---

## Verification Plan

1. 開啟 `/Port` 頁面
2. 點擊樓層切換按鈕 (1F/2F/3F/4F)
3. 確認地圖切換
4. 確認站點只顯示該樓層
