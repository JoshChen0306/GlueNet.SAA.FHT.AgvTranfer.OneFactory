# 路線權限管理系統實作計畫

## 目標
實作分離式路線權限管理系統，讓管理員可以透過 UI 為使用者指派多個派送路線權限。

---

## 路線定義（客戶需求）

| # | RouteId | 路線名稱 | SourceAreas | TargetAreas |
|---|---------|----------|-------------|-------------|
| 1 | ROUTE_1F_3F | 1F → 3F 鑽針區 | G | J |
| 2 | ROUTE_2F_FORMING | 2F 成型區運輸 | M, T, Q, R | O, P, S, N |
| 3 | ROUTE_2F_VCUT | 2F 雷雕區 → Vcut | M | T |
| 4 | ROUTE_3F_1F | 3F 鑽針區 → 1F | J | G |
| 5 | ROUTE_3F_4F | 3F 品檢區 → 4F | I | L |
| 6 | ROUTE_2F_4F | 2F 成型後 → 4F | H | K |
| 7 | ROUTE_4F_3F | 4F → 3F 品檢區 | L | I |

> **備註**：1F 站內運輸（A, B, C, D, E, F）保持現狀，不納入路線權限管控。

---

## 資料表設計

### [NEW] pRoute - 路線定義表

```sql
CREATE TABLE [dbo].[pRoute] (
    [RouteId]      NVARCHAR(30) NOT NULL PRIMARY KEY,
    [RouteName]    NVARCHAR(50) NOT NULL,
    [SourceFloor]  NVARCHAR(10) NULL,
    [TargetFloor]  NVARCHAR(10) NULL,
    [RouteType]    NVARCHAR(20) NULL,       -- 'CROSS_FLOOR' 或 'INTERNAL'
    [SourceAreas]  NVARCHAR(100) NULL,      -- 起點區域，逗號分隔，如 'J,I'
    [TargetAreas]  NVARCHAR(100) NULL,      -- 終點區域，逗號分隔，如 'A,G'
    [ControlFlag]  NVARCHAR(1) DEFAULT 'Y', -- Y=啟用, N=停用
    [SortOrder]    INT DEFAULT 0
);
```

### [NEW] pUserRoute - 使用者路線權限表

```sql
CREATE TABLE [dbo].[pUserRoute] (
    [UserId]  NVARCHAR(20) NOT NULL,
    [RouteId] NVARCHAR(30) NOT NULL,
    PRIMARY KEY ([UserId], [RouteId])
);
```

### 預設資料

```sql
-- 路線定義（客戶需求 7 條）
INSERT INTO pRoute VALUES ('ROUTE_1F_3F', '1F → 3F 鑽針區', '1F', '3F', 'CROSS_FLOOR', 'G', 'J', 'Y', 1);
INSERT INTO pRoute VALUES ('ROUTE_2F_FORMING', '2F 成型區運輸', '2F', '2F', 'INTERNAL', 'M,T,Q,R', 'O,P,S,N', 'Y', 2);
INSERT INTO pRoute VALUES ('ROUTE_2F_VCUT', '2F 雷雕區 → Vcut', '2F', '2F', 'INTERNAL', 'M', 'T', 'Y', 3);
INSERT INTO pRoute VALUES ('ROUTE_3F_1F', '3F 鑽針區 → 1F', '3F', '1F', 'CROSS_FLOOR', 'J', 'G', 'Y', 4);
INSERT INTO pRoute VALUES ('ROUTE_3F_4F', '3F 品檢區 → 4F', '3F', '4F', 'CROSS_FLOOR', 'I', 'L', 'Y', 5);
INSERT INTO pRoute VALUES ('ROUTE_2F_4F', '2F 成型後 → 4F', '2F', '4F', 'CROSS_FLOOR', 'H', 'K', 'Y', 6);
INSERT INTO pRoute VALUES ('ROUTE_4F_3F', '4F → 3F 品檢區', '4F', '3F', 'CROSS_FLOOR', 'L', 'I', 'Y', 7);

-- 管理員擁有所有路線權限
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_1F_3F');
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_2F_FORMING');
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_2F_VCUT');
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_3F_1F');
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_3F_4F');
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_2F_4F');
INSERT INTO pUserRoute VALUES ('1', 'ROUTE_4F_3F');
```

---

## Proposed Changes

### 後端 - Model

#### [NEW] pRoute.cs
```csharp
public class pRoute
{
    public string RouteId { get; set; }
    public string RouteName { get; set; }
    public string SourceFloor { get; set; }
    public string TargetFloor { get; set; }
    public string RouteType { get; set; }
    public string SourceAreas { get; set; }
    public string TargetAreas { get; set; }
    public string ControlFlag { get; set; }
    public int? SortOrder { get; set; }
}
```

#### [NEW] pUserRoute.cs
```csharp
public class pUserRoute
{
    public string UserId { get; set; }
    public string RouteId { get; set; }
}
```

#### [MODIFY] agvDB_1400004Context.cs
- 新增 `DbSet<pRoute>` 和 `DbSet<pUserRoute>`
- 新增 Entity 設定

---

### 後端 - Controller

#### [NEW] RouteController.cs
新增路線權限管理 API：

| API | 說明 |
|-----|------|
| `GET /Route/Index` | 路線權限管理頁面 |
| `GET /Route/GetAllRoutes` | 取得所有路線定義 |
| `GET /Route/GetUserRoutes?userId=xxx` | 取得使用者的路線權限 |
| `POST /Route/SaveUserRoutes` | 儲存使用者路線權限 |

#### [MODIFY] DispatchController.cs
修改 `Index()` 方法：
- 移除原本的 switch-case 硬編碼邏輯
- 改為查詢 `pUserRoute` 取得使用者的路線權限
- 根據路線權限動態篩選可用區域

---

### 前端 - View

#### [NEW] Views/Route/Index.cshtml
路線權限管理頁面，包含：
- 使用者下拉選單
- 路線權限勾選清單
- 儲存按鈕

#### [MODIFY] Views/Dispatch/Index.cshtml
- 前端區域篩選邏輯需配合後端權限調整

---

## UI 設計概念

```
┌─────────────────────────────────────────────┐
│  路線權限管理                                │
├─────────────────────────────────────────────┤
│  使用者：[  王小明  ▼]                       │
├─────────────────────────────────────────────┤
│  派送路線權限：                              │
│  ☑ 1F → 3F 鑽針區                           │
│  ☑ 2F 成型區運輸                            │
│  ☐ 2F 雷雕區 → Vcut                         │
│  ☐ 3F 鑽針區 → 1F                           │
│  ☐ 3F 品檢區 → 4F                           │
│  ☐ 2F 成型後 → 4F                           │
│  ☐ 4F → 3F 品檢區                           │
├─────────────────────────────────────────────┤
│              [儲存]  [取消]                  │
└─────────────────────────────────────────────┘
```

---

## 權限驗證邏輯

```csharp
// 取得使用者的路線權限
var userRoutes = _DBContext.pUserRoute
    .Where(ur => ur.UserId == userId)
    .Join(_DBContext.pRoute, ur => ur.RouteId, r => r.RouteId, (ur, r) => r)
    .ToList();

// 取得使用者可用的所有區域
var allowedAreas = userRoutes
    .SelectMany(r => (r.SourceAreas + "," + r.TargetAreas).Split(','))
    .Distinct()
    .ToList();

// 過濾區域選單
filterAreas = areas.Where(item => allowedAreas.Contains(item.Value));
```

---

## Verification Plan

### 測試案例

| 案例 | 操作 | 預期結果 |
|------|------|----------|
| 1 | 使用者只有 1F→3F 權限，開啟派送頁面 | 只看到 G, J 區域 |
| 2 | 使用者有多路線權限 | 看到所有對應區域（不重複） |
| 3 | 使用者無任何路線權限 | 無法派送任何任務 |
| 4 | 管理員修改使用者權限後 | 使用者重新登入後生效 |

---

## 實作順序

1. 建立資料庫表和預設資料
2. 建立 Entity Model
3. 修改 DbContext
4. 建立 RouteController
5. 建立路線權限管理 UI
6. 修改 DispatchController 權限邏輯
7. 測試驗證
