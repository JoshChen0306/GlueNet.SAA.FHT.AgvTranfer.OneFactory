# FHT 一廠 AGV 派送 — 現場測試腳本

> 專案：GlueNet.SAA.FHT.AgvTranfer.OneFactory（高技一廠）
> 產出日期：2026-05-27
> 測試依據：實際生效的 `WebGui/SCP/appsettings.json` 設定
> 資料三層流向：`oNeed`（需求）→ `oRequire`（配對）→ `oMission`（派給 AGV）
> `oPort.HaveFlag`：`0`=空架 / `1`=空板 / `3`=料盤

---

## 現場資訊

| 項目 | 內容 |
|------|------|
| 測試日期 |  |
| 測試人員 |  |
| 環境（測試機 / 正式機） |  |
| ACC / RCS 版本 |  |
| WebGui 版本 |  |

---

## 0. 測試前環境確認（前置，必做）

| # | 檢查項 | 預期 | 實際 |
|---|--------|------|------|
| 0-1 | DB 連線（appsettings 預設指向 `DESKTOP-2I3FKA2` / `agvDB_1400004_1`，**現場需改成正式 DB**） | 可連線 | ☐ |
| 0-2 | `AutoDispatch.Enabled` | `true`、間隔 30s | ☐ |
| 0-3 | `MyConfig.ShowRackIdField` | `false`（貨架條碼欄位隱藏） | ☐ |
| 0-4 | 啟用區域 `Area` | A / B / K / L / M | ☐ |
| 0-5 | ACC（HikAGVWebAPI）與 RCS（海康 AGV）連線正常、AGV 電量足 | 連線 OK | ☐ |
| 0-6 | 測試帳號路線權限 `pUserRoute` / `pRoute`（DISPATCH 與 RELEASE） | 已設定 | ☐ |

樓層 / 區域對照（依 `FloorSettings`）：

- **FHT1-1F（1F）**：A（供貨區）、B（下料區）、C
- **FHT1-3F（3F）**：K（左側上料區）、L（右側下料區）、M（暫存區）

---

## 流程 1：平板手動派送（一般派送，InsertoNeed）

**入口**：派送頁面 `/Dispatch`，選樓層 → 選起點區 → 選站點 → 送出
**前置**：起點站 `HaveFlag=3`（有料盤）；登入帳號具 DISPATCH 路線權限

| # | 步驟 | 預期結果 | 實際 |
|---|------|----------|------|
| 1-1 | 登入後進派送頁 | 起點區下拉只顯示帳號 DISPATCH 路線的 SourceAreas，樓層只顯示授權樓層 | ☐ |
| 1-2 | 選有料的起點站、選終點、按送出 | `oNeed` 新增一筆，`TaskSource=Web`、`AssignFlag` 空 | ☐ |
| 1-3 | 等 svrPair 配對（約數秒） | 產生 `oRequire`（AssignFlag=Y），再產生 `oMission` | ☐ |
| 1-4 | 觀察 AGV | AGV 前往起點取料 → 送終點 | ☐ |
| 1-5 | 完成後查狀態頁 | `oMission.OkFlag=Y`、起點 `HaveFlag` 歸 0、終點變有料 | ☐ |

---

## 流程 2：貨架條碼欄位隱藏驗證（最新 commit `5aabbec`，重點）

**目的**：`ShowRackIdField=false` 時前端隱藏「貨架條碼」欄位，送出固定帶 `-1` 哨兵值，仍要通過後端必填驗證

| # | 步驟 | 預期結果 | 實際 |
|---|------|----------|------|
| 2-1 | 開派送 / 登記畫面 | **看不到**「貨架條碼」輸入欄 | ☐ |
| 2-2 | 完成一次登記 / 派送送出 | 後端不因 RackId 必填而擋下（哨兵 `-1` 通過）；`oNeed.RackId` 寫入哨兵值 | ☐ |
| 2-3 | （回歸）暫時把設定改 `true` 重啟 | 欄位重新出現、需手動輸入貨架條碼 | ☐ |

---

## 流程 3：登記帳料 / 清除 / 標記空板（RegisterLot / ClearLot / MarkEmptyTray）

| # | 步驟 | 預期結果 | 實際 |
|---|------|----------|------|
| 3-1 | 在空架站點登記工單（A/B/K/L/M） | `HaveFlag=3`、寫入 WorkOrder / PutTime | ☐ |
| 3-2 | K / L 區登記但不填貨架條碼 | 後端擋下「請輸入貨架條碼」（K/L 為 RackId 必填區）※ 若 `ShowRackIdField=false` 由哨兵值帶過，須現場確認此衝突 | ☐ |
| 3-3 | M 區登記時勾「V Cut 專用」 | 工單尾端附加 `^VCUT` | ☐ |
| 3-4 | 對有進行中任務的站點按登記 / 清除 | 擋下「此站點有進行中的派送任務」 | ☐ |
| 3-5 | 對料盤(3)站點按「清除物料」 | `HaveFlag=0`、工單 / 貨架 / 時間清空 | ☐ |
| 3-6 | 對料盤(3)站點按「標記空板」 | `HaveFlag=1`、工單清空、**RackId 保留** | ☐ |

---

## 流程 4：Release 空板回送（依起點區自動找終點）

**前置**：起點站須先「標記空板」(`HaveFlag=1`)；帳號須有該區 **RELEASE** 路線權限

| # | 起點區 | 預期終點（程式邏輯） | 實際 |
|---|--------|----------------------|------|
| 4-1 | **K**（3F 上料） | → L 區空位（3F 下料） | ☐ |
| 4-2 | **M**（3F 暫存） | → A 區空位（1F 備貨，**跨樓層**） | ☐ |
| 4-3 | **B**（1F 下料） | → A 區空位（1F 備貨） | ☐ |

異常檢核：

| # | 情境 | 預期 | 實際 |
|---|------|------|------|
| 4-4 | 起點不是空板(`HaveFlag≠1`)就按 Release | 擋下「請先標記為空板」 | ☐ |
| 4-5 | 同站點重複 Release | 擋下「此站點已有待處理的派送任務」 | ☐ |
| 4-6 | 目標區無空位 | 擋下對應「沒有可放置的空位」訊息 | ☐ |
| 4-7 | 無 RELEASE 權限帳號按 Release | HTTP 403「您沒有此區域的回送權限」 | ☐ |

> 註：程式另有 G→J、I→L(L1–L4) 分支，但這兩區不在本廠 `Area` 設定內，現場若沒佈這些區則不列入測試。

---

## 流程 5：自動派送 M → K（AutoDispatchService，背景每 30s）

**前置**：`AutoDispatch.Enabled=true`

| # | 步驟 | 預期結果 | 實際 |
|---|------|----------|------|
| 5-1 | M 區放一個料盤(`HaveFlag=3`)，K 區留空位(`HaveFlag=0`) | 30s 內自動產生 `oNeed`，`TaskSource=Auto` | ☐ |
| 5-2 | M 區多個料盤 | 依 `PutTime` FIFO 排序，配對 K 區空位（取兩者較小數量） | ☐ |
| 5-3 | M 區料盤已有在途任務 | **不重複**派送（起 / 終點去重） | ☐ |
| 5-4 | K 區無空位 | 不派送，等有空位才派 | ☐ |
| 5-5 | 看 log | `AutoDispatch: M.. → K.. (PutTime: ..)` | ☐ |

---

## 流程 6：派送狀態顯示 / 取消（UpdateDispatch / DeleteoNeed）

| # | 步驟 | 預期結果 | 實際 |
|---|------|----------|------|
| 6-1 | 開派送狀態列表 | 顯示 oRequire：執行中(藍) / 已派車(綠) / 取消(灰) / 異常(紅) | ☐ |
| 6-2 | 對「已派車未執行」任務按取消 | `oRequire` 與 `oMission` 的 `OkFlag` 一併設 `C` | ☐ |
| 6-3 | AGV 已取料後才取消 | 確認現場 AGV 實際行為（是否中途返回 / 續走）— **重點觀察** | ☐ |

---

## 流程 7：AGV 回呼整合（ACC 端 CallBackAPI，端到端）

| # | RCS 回呼 | 預期 DB 變化 | 實際 |
|---|----------|--------------|------|
| 7-1 | `start` | 任務開始、ubActivation 記錄 | ☐ |
| 7-2 | `outbin`（取出） | 起點 `HaveFlag` 更新 | ☐ |
| 7-3 | `end` | `oMission.OkFlag=Y`、終點變有料 | ☐ |
| 7-4 | `cancel` | `oMission.OkFlag=C`；若有 ParentTaskDateTime 連動取消 | ☐ |

---

## 流程 8：跨樓層自動化（ACC 端 CrossFloorManager，選測）

| # | 情境 | 預期 | 實際 |
|---|------|------|------|
| 8-1 | AGV 閒置超過 `idleReturnTimeoutSeconds` | 自動產生 `IDLE_RETURN` 任務，回到 `idleReturnFloor` | ☐ |
| 8-2 | 需跨樓層取料 | 產生 `CROSS_FLOOR_DISPATCH` 預調度任務（先把車調到對的樓層） | ☐ |
| 8-3 | 預調度完成後短時間內 | 冷卻期（預設 30s）內不重派同一 parent | ☐ |

> 8 的閾值參數（timeout / floor / cooldown）在 ACC 設定檔，建議現場先確認實際值再測。

---

## 建議測試順序（由簡到繁）

1. 環境確認（0）
2. 貨架欄位隱藏（流程 2，新功能優先）
3. 登記 / 清除 / 空板（流程 3）
4. 手動派送端到端（流程 1 + 7）
5. Release 回送（流程 4）
6. 自動派送 M→K（流程 5）
7. 取消 / 異常（流程 6）
8. 跨樓層（流程 8，選測）

---

## 現場注意事項

1. `appsettings.json` 的 DB 連線目前指向開發機 `DESKTOP-2I3FKA2`，現場要改成正式 DB。
2. 流程 3-2 的「K/L 區貨架條碼必填」與「`ShowRackIdField=false` 隱藏欄位」可能有衝突，建議現場優先驗證這一條。
3. 每個案例請同步檢視 `oNeed` / `oRequire` / `oMission` 三張表與 `oPort.HaveFlag` 變化。

---

## 測試結果總結

| 流程 | 通過 / 失敗 / 未測 | 備註 |
|------|---------------------|------|
| 0 環境確認 |  |  |
| 1 手動派送 |  |  |
| 2 貨架欄位隱藏 |  |  |
| 3 登記 / 清除 / 空板 |  |  |
| 4 Release 回送 |  |  |
| 5 自動派送 M→K |  |  |
| 6 狀態 / 取消 |  |  |
| 7 AGV 回呼 |  |  |
| 8 跨樓層 |  |  |
