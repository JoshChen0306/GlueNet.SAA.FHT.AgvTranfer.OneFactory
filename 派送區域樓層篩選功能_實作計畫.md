# 派送區域樓層篩選功能

## 需求說明

1. 新增樓層選擇器，選擇樓層後只顯示該樓層的區域
2. **樓層選擇連動地圖顯示**（切換不同樓層地圖）
3. **權限篩選優先**：先依使用者權限過濾區域，再依樓層過濾

## 樓層與區域對應

| 樓層 | 區域 | 地圖檔案 |
|------|------|----------|
| 1F | A, C, D, F, EE | `/img/FHT2-1F.png` |
| 2F | H | `/img/FHT2-2F.png` |
| 3F | J | `/img/FHT2-3F.png` |
| 4F | K | `/img/FHT2-4F.png` |

---

## Proposed Changes

### 後端設定

#### [MODIFY] [appsettings.json](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/appsettings.json)

新增樓層與區域的對應設定 `FloorArea`：

```json
"FloorArea": {
  "1F": ["A", "C", "D", "F", "EE"],
  "2F": ["H"],
  "3F": ["J"],
  "4F": ["K"]
}
```

---

### 後端 Controller

#### [MODIFY] [DispatchController.cs](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/Controllers/DispatchController.cs)

1. 在 `Index()` 方法中新增 `FloorArea` 設定到 ViewBag
2. 新增樓層列表 `FloorList` 到 ViewBag

```csharp
// 新增樓層列表
ViewBag.FloorList = new List<SelectListItem>
{
    new SelectListItem { Value = "1F", Text = "1F" },
    new SelectListItem { Value = "2F", Text = "2F" },
    new SelectListItem { Value = "3F", Text = "3F" },
    new SelectListItem { Value = "4F", Text = "4F" }
};

// 傳遞樓層區域對應到前端
ViewBag.FloorArea = _configuration.GetSection("FloorArea").Get<Dictionary<string, string[]>>();
```

---

### 前端 View

#### [MODIFY] [Index.cshtml](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/Views/Dispatch/Index.cshtml)

在 Area 下拉選單前新增 Floor 下拉選單：

```html
<!-- 新增樓層選擇 -->
<div class="row mb-3">
    <label class="col-2 col-form-label">樓層</label>
    <div class="col-10">
        <select class="form-select w-75" name="Floor" id="Floor" required>
            <option value="" disabled selected hidden>請選擇樓層</option>
            @foreach (var floor in ViewBag.FloorList)
            {
                <option value="@floor.Value">@floor.Text</option>
            }
        </select>
    </div>
</div>
```

並在頁面底部注入樓層區域對應的 JavaScript 變數：

```html
<script>
    var floorAreaMap = @Html.Raw(Json.Serialize(ViewBag.FloorArea));
</script>
```

---

### 前端 JavaScript

#### [MODIFY] [Dispatch.js](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/wwwroot/js/Dispatch.js)

新增樓層選擇事件，包含**地圖切換**和**權限後篩選**：

```javascript
// 樓層與地圖區域對應 (對應 Map.js 的 loadMapData 參數)
var floorToMapArea = {
    "1F": "FHT2-1F",
    "2F": "FHT2-2F",
    "3F": "FHT2-3F",
    "4F": "FHT2-4F"
};

// 選擇樓層後篩選 Area 選項並切換地圖
$("#Floor").on("change", function () {
    var selectedFloor = $(this).val();
    var allowedAreas = floorAreaMap[selectedFloor] || [];
    
    // 1. 切換地圖 - 使用既有的 loadMapData 函數 (來自 Map.js)
    if (typeof loadMapData === 'function' && floorToMapArea[selectedFloor]) {
        loadMapData(floorToMapArea[selectedFloor]);
    }
    
    // 2. 重置 Area 和後續選項
    $("#Area").val('');
    $("#BeginStation").val('');
    $("#EndStation").val('');
    $(".Site").prop("disabled", true);
    
    // 3. 顯示/隱藏符合樓層的 Area 選項（已經過權限篩選）
    $("#Area option").each(function () {
        var areaValue = $(this).val();
        if (areaValue === "" || allowedAreas.includes(areaValue)) {
            $(this).show();
        } else {
            $(this).hide();
        }
    });
    
    // 4. 啟用 Area 選擇
    $("#Area").prop("disabled", false);
});

// 初始時停用 Area (需先選樓層)
$("#Area").prop("disabled", true);
```

#### [MODIFY] [Map.js](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/wwwroot/js/Map.js)

將 `loadMapData` 函數 **export** 供 Dispatch.js 使用：

```javascript
// 在檔案開頭加入 export
export function loadMapData(area) {
    // ... 既有程式碼
}
```

> [!NOTE]
> Area 下拉選單由後端根據權限預先篩選。前端只在這些選項中再依樓層篩選。

---

## Verification Plan

### Manual Verification

1. 開啟 Dispatch 頁面 (`/Dispatch`)
2. 確認頁面出現「樓層」下拉選單
3. 選擇 **1F**：
   - ✅ 地圖切換為 `FHT2-1F.png`
   - ✅ 區域只顯示：A, C, D, F, EE（依權限篩選後）
4. 選擇 **3F**：
   - ✅ 地圖切換為 `FHT2-3F.png`
   - ✅ 區域只顯示：J
5. 完成一次完整派送任務
