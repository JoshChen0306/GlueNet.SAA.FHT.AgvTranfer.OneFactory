# 工作計畫：充電樁位置設定檔化

- 建立日期：2026-06-02
- 分析力度：標準模式
- 對應規格：`spec.md`、`plan.md`
- 採用方案：A（抽出可測試 helper）

## Commit 策略

- 子任務 1 拆兩個 commit：`test:` 補既有 ConvertX/Y 測試 → `refactor:` 純搬移到 MapCoordinateConverter（0 邏輯變更）。
- 子任務 2 起為 `feat:`。
- 設定檔/View 變更獨立 commit，message 標明影響範圍。

## 執行順序

P1 先（子任務 1 → 2，建立可測試基礎），P2 後（3 → 4 → 5，串接顯示）。

## 單一任務完成 4 道 Gate

每個子任務完成、標記 `[x]` 之前必須全部通過：

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | 對應專案建置 | 0 error（warning 允許） |
| 2. Test | TDD: 對應測試 / 一般: 回歸測試 | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 對應驗收標準 | 全部滿足 |
| 4. Regression | 若屬重構 / 效能優化 | 完整測試套件 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者，不得跳過。

## 跨優先順序驗證點

### 🚦 驗證點 #1：P1 → P2（在進入第一個 P2 任務前執行）

完成的 P1 任務：子任務 1、子任務 2

驗證步驟：
1. 重新執行 P1 任務新增的所有測試（`MapCoordinateConverter`、`ChargingStationProvider`）
2. 重新執行既有測試 + 新增測試 → 0 失敗
3. 抽查 spec.md 子任務 1、2 驗收標準全部滿足（特別是 3F `swapXY` 與容錯）
4. 抽查整合行為：`ConvertX/ConvertY` 搬移後，既有庫位/車百分比輸出與重構前一致

通過 → 進入 P2 任務
失敗 → 停下找使用者討論修復方案，修復後再進入 P2

## 工作項目

- [x] [P1] 🧪 抽取 `ConvertX/ConvertY` → `MapCoordinateConverter`（共用換算工具）
  ├─ TDD 原因：共用元件（庫位/車/充電樁共用）＋ 重構需回歸保護鎖定既有行為
  ├─ 測試分類：Happy Path / Boundary
  ├─ 步驟：補測試鎖 ConvertX/Y 行為 → 純搬移到 MapCoordinateConverter → CommonController + PortController 改呼叫 → 重跑測試結果一致
  ├─ ⚠️ 影響範圍掃描補充：`PortController.cs:222/239` 也有一份「完全相同」的 ConvertX/ConvertY 重複碼，本任務一併改為呼叫共用工具（消除重複，邏輯 0 變更）
  └─ 預估測試案例數：5~6

- [x] [P1] 🧪 新增 `ChargingStationProvider.Get(IConfiguration, area)`
  ├─ TDD 原因：明確輸入輸出（設定字串 → List<Position>）＋ 多例外路徑（swapXY、格式錯誤、空設定）
  ├─ 測試分類：Happy Path / Edge Case / Error
  ├─ 內容：解析 `"X,Y,角度"` → swapXY（讀 AgvSetting，與 GetTrac 一致）→ MapCoordinateConverter 換算 → Position(Left,Bottom,Transform=rotate(角度deg))
  └─ 預估測試案例數：6~8

- [x] [P2] 於 `appsettings.json` 新增 `ChargingStations` 區段
  └─ 非 TDD：純設定檔。含 `FHT1-1F`、`FHT1-3F` 虛擬座標 + `TODO 待替換真實座標` 註記

- [x] [P2] `_MapPartial.cshtml` 改為迴圈渲染充電樁
  └─ 非 TDD：純 UI/View。`@foreach (var cs in ViewBag.ChargingStations)` 取代寫死區塊，移除 `area=="FHT1-1F"` 條件，套 `bottom/left/transform`，圖示 `ChargingStation-off.png`

- [x] [P2] `CommonController.ShowMap` 接線
  └─ 一般：呼叫 `ChargingStationProvider.Get` → `ViewBag.ChargingStations`；回歸確認既有庫位/車不受影響

## 🧪 測試案例矩陣

### 子任務 1：MapCoordinateConverter

| # | 案例名稱 | 分類 | 輸入摘要 | 預期結果 | 優先序 |
|---|---------|------|---------|---------|--------|
| 1 | ConvertX 下界 | Boundary | FHT1-1F, x=minX(198700) | "17%" | P0 |
| 2 | ConvertX 上界 | Boundary | FHT1-1F, x=maxX(214000) | "91.5%" | P0 |
| 3 | ConvertY 下界 | Boundary | FHT1-1F, y=minY(196350) | "14%" | P0 |
| 4 | ConvertX 一般值 | Happy Path | FHT1-1F, x=204139 | 約 "43.48%" | P1 |
| 5 | ConvertY 一般值 | Happy Path | FHT1-1F, y=197533 | 範圍內百分比 | P1 |
| 6 | 搬移前後一致 | Happy Path | 同輸入比對重構前後 | 輸出完全相同 | P0 |

### 子任務 2：ChargingStationProvider

| # | 案例名稱 | 分類 | 輸入摘要 | 預期結果 | 優先序 |
|---|---------|------|---------|---------|--------|
| 1 | 1F 單顆 | Happy Path | ChargingStations:FHT1-1F=["X,Y,0"] | 1 個 Position，Transform rotate(0deg) | P0 |
| 2 | 1F 多顆 | Happy Path | 3 筆設定 | 回傳 3 個 Position | P0 |
| 3 | 3F swapXY | Edge Case | FHT1-3F swapXY=true, 一筆 | 座標 X/Y 交換後換算 | P0 |
| 4 | 該樓層無設定 | Edge Case | 無 ChargingStations:area | 空清單 Count=0 | P0 |
| 5 | 格式錯誤略過 | Error | "X,Y"（缺角度）/ 非數值 | 略過該筆、不拋例外 | P0 |
| 6 | 角度旋轉 | Happy Path | "X,Y,90" | Transform = rotate(90deg) | P1 |

## 測試骨架位置

- `SCP.Tests/Helpers/MapCoordinateConverterTests.cs`
- `SCP.Tests/Helpers/ChargingStationProviderTests.cs`
（`SCP.Tests` 不存在 → 子任務 1 開工時自動建立並加入 `SCP.sln`）
