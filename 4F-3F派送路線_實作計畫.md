# 4F-3F 派送路線 實作計畫

本文件描述 4F 烘烤後區域與 3F 品檢區之間的派送流程。

---

## 站點對應表

| 區域 | 系統代號 | 站點範圍 | 用途 |
|------|----------|----------|------|
| 4F 烘烤後 | L | L1~L4 | 烘烤後出貨/入貨 |
| 3F 品檢區 | I | I1~In | 品質檢驗 |

---

## 派送路線總覽

| 路線 | 起點 | 終點 | 場景 | 工單 |
|------|------|------|------|------|
| 1 | L (4F 烘烤後) | I (3F 品檢區) | 正常派送 | **必填** |
| 2 | I (3F 品檢區) | L (4F 烘烤後) | Release 回送 | 不需要 |
| 3 | I (3F 品檢區) | L (4F 烘烤後) | NG 回送 | **必填** |

---

## ⚠️ 重要：回送物料不可再次派送

### 設計原則

**回送物料**（從 3F 品檢區回到 L1-L4 的物料）**不可**再次出現在 4F → 3F 的正常派送清單中。

### 物料標記定義

| 標記 | WorkOrder 格式 | 意義 | 可派送 |
|------|---------------|------|--------|
| 無標記 | `FHT^N01^...` | 正常物料（新烘烤完成） | ✅ 可派送 |
| `^RETURN` | `^RETURN` | Release 回送（空板） | ❌ 不可派送 |
| `^NG` | `xxx^NG` | NG 回送（品檢失敗） | ❌ 不可派送 |

### 過濾規則

**L 區起點過濾（正常派送）**：只顯示**不含** `^RETURN` 和 `^NG` 的物料

```javascript
case "L":
    // L 區（4F烘烤後）作為起點：只顯示 L1-L4 有料且非回送物料
    $('#BeginStation option').filter(function () {
        var tracname = $(this).val();
        if (!tracname) return false;
        
        var station = stationCache[tracname];
        if (!station) return false;
        
        // L1-L4 且有料 (HaveFlag = 3)
        var portNum = parseInt(tracname.replace("L", ""));
        if (portNum < 1 || portNum > 4) return false;
        if (station.haveFlag !== "3") return false;
        
        // ★ 排除回送物料 ★
        var workOrder = station.workOrder || "";
        if (workOrder.includes("^RETURN") || workOrder.includes("^NG")) {
            return false;  // 回送物料不可再次派送
        }
        
        return true;
    }).show();
    break;
```

### Release 回送標記邏輯

當 Release 從 I 區回送到 L 區時，需在 L 區站點寫入 `^RETURN` 標記：

```csharp
// Release 任務完成後，AGV Callback 更新 L 區站點
// WorkOrder = "^RETURN"  // 標記為回送空板
```

---

## 流程 1：4F → 3F（正常派送）

### 操作流程

| 步驟 | 使用者操作 | 系統行為 |
|------|----------|----------|
| 1 | 在 L1-L4 站點登記物料（工單 + 貨架） | HaveFlag=3, WorkOrder=工單 |
| 2 | 選擇樓層 = **4F** | 派送區域選項：L（烘烤後） |
| 3 | 選擇派送區域 = **L（烘烤後）** | - |
| 4 | - | 起點下拉：只顯示 L1-L4 且 HaveFlag=3 的站點 |
| 5 | 選擇起點（例：L1） | - |
| 6 | - | **終點自動帶出**：I 區第一個 HaveFlag=0 的空位 |
| 7 | 點擊「確認派車」 | 建立派送任務 (L → I) |

### 終點自動選擇邏輯

```javascript
// L 區派送：終點自動選擇 I 區空架
case "L":
    autoSelectEndStation("I", "0", "N");  // L區（4F烘烤後）→ I區（3F品檢區）
    break;
```

---

## 流程 2：3F → 4F Release（品檢成功）

### 操作流程

| 步驟 | 使用者操作 | 系統行為 |
|------|----------|----------|
| 1 | 貨物送達 I 區，人員取走物料 | - |
| 2 | 點擊 I 區站點 | 彈出站點操作視窗 |
| 3 | 點擊「標記空板」 | HaveFlag=1, WorkOrder 清除, **RackId 保留** |
| 4 | 點擊「Release」 | - |
| 5 | - | 系統查詢 L1-L4 可放置空位 |
| 6a | (有空位) | 建立派送任務 (I → L)，帶入 RackId，WorkOrder=^RETURN |
| 6b | (無空位) | 彈出提示「4F 烘烤後 (L1-L4) 沒有可放置的空位」 |

### Release 判斷邏輯 (DispatchController.cs)

```csharp
else if (stationArea == "I")
{
    // I 區（3F品檢區）→ 回送到 L 區（4F烘烤後）L1-L4
    emptySlot = _DBContext.oPort
        .Where(p => p.Block == "L" &&
                    p.HaveFlag == "0" &&
                    (p.BgnToEnd == null || p.BgnToEnd == "") &&
                    p.UseFlag == "Y" &&
                    p.Port >= 1 && p.Port <= 4 &&  // 只選 L1-L4
                    !pendingEndStations.Contains(p.StationNo))
        .OrderByDescending(p => p.Priority)
        .ThenBy(p => p.Port)
        .FirstOrDefault();

    if (emptySlot == null)
    {
        return BadRequest(new { message = "4F 烘烤後 (L1-L4) 沒有可放置的空位" });
    }
}
```

---

## 流程 3：3F → 4F NG 回送（品檢失敗）

### 操作流程

| 步驟 | 使用者操作 | 系統行為 |
|------|----------|----------|
| 1 | 貨物送達 I 區，人員取走物料檢驗 | - |
| 2 | 檢驗 NG，需退回 4F | - |
| 3 | 點擊 I 區站點 → 「標記空板」 | HaveFlag=1 |
| 4 | 再次點擊 I 區站點 → 「物料登記」 | - |
| 5 | 輸入工單和貨架 | HaveFlag=3, WorkOrder=xxx^NG（**自動加上**） |
| 6 | 選擇派送區域 = **I（品檢區）** | - |
| 7 | - | 起點下拉：只顯示 I 區 HaveFlag=3 的站點 |
| 8 | 選擇起點（例：I1） | - |
| 9 | - | **終點自動帶出**：L1-L4 第一個空位 |
| 10 | 點擊「確認派車」 | 建立派送任務 (I → L) |

### I 區站點操作按鈕邏輯

| HaveFlag | 狀態 | 顯示按鈕 |
|----------|------|----------|
| 0 | 空架 | 📋 **物料登記** |
| 1 | 空板 | 📋 **物料登記** + 🚚 **Release 回送** |
| 3 | 料盤 | 📦 **標記空板** |

### WorkOrder 標記與顏色

| 標記 | 意義 | UI 顏色 |
|------|------|---------|
| 無標記 | 正常物料 | 🟢 **綠色** |
| `^RETURN` | 回送空板 | 🟡 **黃色** |
| `^NG` | 品檢失敗物料 | 🟠 **橙色** |

---

## 程式碼修改清單

### 1. 設定檔 (appsettings.json)

**FloorArea 確認包含**：
```json
"4F": ["K", "L"],
"3F": ["J", "I"]
```

---

### 2. 前端 (Dispatch.js)

| 修改項目 | 說明 |
|----------|------|
| `validAreas` | 新增 `I` 支援站點操作 |
| `releaseAreas` | 新增 `I` 支援 Release |
| `filterBeginStationOptions` | L 區起點過濾（L1-L4 有料，排除回送物料） |
| `filterBeginStationOptions` | I 區起點過濾（有料） |
| `autoSelectEndStation` | L 區 → I 區終點自動選擇 |
| `autoSelectEndStation` | I 區 → L1-L4 終點自動選擇 |

---

### 3. 後端 (DispatchController.cs)

| 修改項目 | 說明 |
|----------|------|
| `RegisterLot` | I/L 區 RackId 必填驗證 |
| `Release` | I 區 → L1-L4 回送邏輯 |

---

### 4. 後端 (cPair.cs)

```csharp
case "L":   // L區（4F烘烤後）→ I區（3F品檢區）
case "I":   // I區（3F品檢區）→ L區（4F烘烤後）
    WriteLog("05.處理系統配對");
    ProcessoNeedToRequire(dr["ObjStation"].ToString().Substring(0, 1), dr);
    break;
```

---

### 5. 地圖顯示 (Map.js)

新增 NG 物料橙色和 RETURN 回送物料黃色顯示邏輯。

---

## 任務清單

### 1. 設定檔
- [x] **1.1** 確認 FloorArea 包含 I 區和 L 區 ✅

### 2. 前端 (Dispatch.js)
- [x] **2.1** `validAreas` 新增 I ✅
- [x] **2.2** `releaseAreas` 新增 I ✅
- [x] **2.3** L 區起點過濾（L1-L4 有料，排除回送物料）✅
- [x] **2.4** I 區起點過濾（有料）✅
- [x] **2.5** L 區 → I 區終點自動選擇 ✅
- [x] **2.6** I 區 → L1-L4 終點自動選擇 ✅
- [x] **2.7** `openStationLotModal` 支援 I 區（繼承通用邏輯）✅
- [x] **2.8** I 區站點操作按鈕：空架/空板顯示「物料登記」，空板額外顯示「Release」✅

### 3. 後端 (DispatchController.cs)
- [x] **3.1** `RegisterLot` I/L 區 RackId 必填 ✅
- [x] **3.2** `Release` I 區 → L1-L4 邏輯 ✅

### 4. 後端 (cPair.cs)
- [x] **4.1** 新增 I 區和 L 區 case 處理 ✅

### 5. 地圖顯示 (Map.js)
- [x] **5.1** NG 物料橙色顯示 ✅
- [x] **5.2** RETURN 回送物料黃色顯示 ✅

### 6. 回送物料標記
- [x] **6.1** Release 回送時寫入 `^RETURN` 標記 ✅
- [x] **6.2** L 區起點過濾排除 `^RETURN` 和 `^NG` 物料 ✅
