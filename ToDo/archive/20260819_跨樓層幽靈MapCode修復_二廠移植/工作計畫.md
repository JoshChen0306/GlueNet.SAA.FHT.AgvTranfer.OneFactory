# 一廠跨樓層幽靈 MapCode 修復（自二廠移植）

## 建立日期
2026-08-19

## 狀態
**方向已確定，可開工。** 移植來源：二廠 commit `dcd7633`、`10a1062`、`2b95c4e`、`2dad4d3`

## 來源
二廠 2026-08-19 客訴修復完成後，經逐檔 diff 確認一廠有**逐字相同**的缺陷。
背景、差異比對與驗收標準見同資料夾 `spec.md`，移植決策見 `plan.md`。

## 已確定的移植決策

| 項目 | 決策 | 依據 |
|------|------|------|
| 測試檔的 `BB`/`DD`/`FF` 地圖代碼 | **逐字照搬，不改成 AA/CC** | `Resolve()` 全程只做字串比對，mapCode 是不透明字典 key，不查任何 config |
| 測試檔的 `J1`/`H3`/`G1`/`K1` 站點 | **逐字照搬** | 測試用 `new ElevatorSettings()` 走 `ConfigModel.cs:292` DefaultValue，兩廠一字不差 |
| `CreateManager()` 的 `mapCodeFloorMapping` 字串 | **逐字照搬** | 一廠既有 `CrossFloorManagerSelectNextMissionTests.cs:250` 已寫死同一字串；新測試以 `currentFloor` 參數直接注入，該字串為惰性參數 |
| `ShuttleMapCodeResolver.cs` 提到二廠地圖的註解 | **保留不動** | 純文件層，改動只製造兩廠永久分歧 |
| 一廠 `Dispatch.cs:169-171` 註解與斷行 | **保留不動** | 一廠既有寫法，強制統一無收益 |
| csproj | **逐行插入，禁止整段覆蓋** | 一廠多 3 個 `<Compile Include>`，整段覆蓋會刪掉 |
| T0 整檔轉 BOM | **不需要** | 已驗：待改的 `Dispatch.cs`/`CrossFloorManager.cs`/兩 csproj 皆為 `efbbbf` |

## 執行順序
T1（裁決器+測試）→ T2（Dispatch 接線）→ T3（決策層閘門+測試）→ 🚦驗證點 → T4（全回歸+交付說明）

與二廠 commit 一一對應，日後兩廠對帳最省力。

## 單一任務完成 4 道 Gate

每個子任務完成、標記 `[x]` 之前必須全部通過：

| Gate | 檢查項 | 判定 |
|------|--------|------|
| 1. Build | 對應專案建置 | 0 error（warning 允許） |
| 2. Test | TDD: 對應測試 / 一般: 回歸測試 | 全部通過 |
| 3. Acceptance | 逐項核對 spec.md 對應驗收標準 | 全部滿足 |
| 4. Regression | 若屬重構 / 效能優化 | 完整測試套件 0 失敗 |

任一 Gate 失敗 → 標 `[!] 阻塞中` + 停下找使用者，不得跳過。

## 跨優先順序驗證點

### 🚦 驗證點 #1：P0 → P1（在進入第一個 P1 任務前執行）

完成的 P0 任務：T1、T2、T3

驗證步驟：
1. 重新執行 T1／T3 新增的全部測試（`ShuttleMapCodeResolverTests`、`CrossFloorManagerFloorTrustTests`）
2. 重新執行既有測試 + 新增測試 → 0 失敗，且既有測試數與 T0 基準數一致
3. 抽查 spec.md 的 AC1~AC11 全部滿足
4. 整合行為抽查：
   - `Update_oShuttle` 全 ACC 僅一處呼叫且在 `Resolve()` 之後（AC13）
   - csproj 未刪除一廠既有 3 個 `<Compile Include>`（AC14）

通過 → 進入 T4
失敗 → 停下找使用者討論修復方案

## 工作拆解計畫

- [x] [P0] 🧪 **T1** 移植 `ShuttleMapCodeResolver` 與其測試 — ✅ `2ee6310`，118 測試全通過，CS0433 風險確認不存在
  ├─ 新增 `ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/ShuttleMapCodeResolver.cs`（逐字，UTF-8 BOM）
  ├─ 新增 `ACC/HikAGVWebAPI/HikAGVWebAPITests/App_Start/ShuttleMapCodeResolverTests.cs`（逐字，12 案例）
  ├─ `HikAGVWebAPI.csproj` 在 `PlcDevice.cs` 之後插 1 行；`HikAGVWebAPITests.csproj` 插 1 行
  ├─ 對應二廠 commit：`dcd7633`
  ├─ 對應 AC：AC1~AC8
  └─ ⚠️ 本任務同時驗證 CS0433 風險：一廠測試專案同時參考 `HikAGVWebAPI` 與 `HikAGV`，
      兩者各自編譯一份 global namespace 的 `AGVStatusData`；一廠既有測試從未用過該型別，
      本次為首次使用。二廠拓樸相同且 Build 0 error，預期無事，但須實跑確認

- [x] [P0] **T2** `Dispatch.UpdateAGVStatus` 接線至裁決器
  ├─ 插入欄位 `_mapCodeResolver`（一廠 `Dispatch.cs:35` 之後）
  ├─ 取代 `UpdateAGVStatus` 的 foreach 主體（一廠 :174-232）為二廠版本
  ├─ **保留一廠 :169-171 既有註解與 Split 斷行**
  ├─ 對應二廠 commit：`10a1062`
  └─ 非 TDD：與 SQLData / HikAGV / config 深度耦合，以 Build + 整合抽查驗證

- [x] [P0] 🧪 **T3** `CrossFloorManager` 樓層可信度閘門 — ✅ `6d7b12b`，128 測試全通過，一廠既有 10 個 SelectNextMission 案例保持通過
  ├─ 取代 `SelectNextMission`（一廠 :268-309）為委派版 + `SelectNextMissionForFloor` + `FindSystemTask`
  ├─ 取代 `DecideNextCrossFloorAction` 的 sameFloorTask 段（一廠 :332-337），加 currentFloor 空值守衛
  ├─ 新增 `CrossFloorManagerFloorTrustTests.cs`（逐字，10 案例）＋ test csproj 插 1 行
  ├─ 對應二廠 commit：`2b95c4e`
  ├─ 對應 AC：AC9~AC11
  └─ 附帶改善：`FindSystemTask` 帶入 `m != null` 防呆（一廠 :273-274 原無，list 含 null 會 NRE）

- [x] [P1] **T4** 完整回歸與交付說明
  ├─ 不帶 filter 跑完整測試套件（既有 + 22 新增）
  ├─ 對應 AC：AC12~AC14
  ├─ 產出交付說明，**必含**「測試站點用的是 `ConfigModel` DefaultValue 而非一廠現場
  │   `FHtSetting.config`，測試綠燈不等於現場站點對照正確」的但書
  └─ 附上 spec.md 的人工驗證清單供現場填寫

## Commit 策略
每個子任務獨立 commit，與二廠訊息格式對齊：
- `fix(acc): 移植二廠ShuttleMapCodeResolver同輪去重與MapCode連續確認`
- `fix(acc): UpdateAGVStatus改為整輪收集裁決後才寫oShuttle`
- `fix(acc): CrossFloorManager樓層不可信時不派發跨樓層任務`
- `docs(todo): 一廠移植完成摘要與現場驗證清單`
