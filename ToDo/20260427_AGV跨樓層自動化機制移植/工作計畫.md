# 工作計畫：AGV 跨樓層自動化機制移植

> 從二廠（GlueNet.SAA.FHT.AgvTranfer.SecondFactory）完整移植 CrossFloorManager 到一廠
> 包含：自動回歸（IDLE_RETURN）+ 預派調度 / 全廠找車（CROSS_FLOOR_DISPATCH）
> 採用方案 A：直接 Copy & Paste，最小調整
> 詳細需求見 `spec.md`，技術方案見 `plan.md`

## 子任務清單

### P0 — DB 基礎建設（先行）

- [x] [P0] 建立 `Deploy_oShuttle_AddUpdateTime.sql` 並執行
  - 新增欄位：`oShuttle.UpdateTime DATETIME NULL`
  - 用途：記錄車輛狀態最後更新時間
  - 驗收：欄位存在，既有資料 NULL 不影響系統
  - **完成紀錄（2026-04-28）**：使用 idempotent 寫法（`IF NOT EXISTS`），DB 驗收 PASS（型別 datetime, NULL）

- [x] [P0] 建立 `Deploy_oShuttle_AddMapCode.sql` 並執行
  - 新增欄位：`oShuttle.MapCode NVARCHAR(20) NULL`
  - 用途：記錄車輛當前所在 MapCode（AA/BB/DD/FF），由 RCS 回報
  - 驗收：欄位存在，oShuttleModel 對應屬性可讀寫
  - **完成紀錄（2026-04-28）**：一廠既有環境此欄位早已存在；本檔以 idempotent 寫法作為新環境部署保險閘，DB 驗收 PASS（型別 nvarchar(20), NULL）

- [x] [P0] 建立 `Deploy_AddParentTaskDateTime.sql` 並執行
  - 新增欄位：`oMission.ParentTaskDateTime NVARCHAR(20) NULL`
  - 用途：預調度任務記錄父任務的 TaskDateTime
  - 驗收：欄位存在，新任務寫入正確
  - **完成紀錄（2026-04-28）**：照搬二廠（idempotent），同步新增 `ubMission.ParentTaskDateTime`（歷史歸檔表 select * 需求），DB 驗收 PASS（型別 varchar(50), NULL）

- [x] [P0] 建立 `Deploy_oTaskTypeRoute.sql` 並執行
  - 新建表：`oTaskTypeRoute`（Id / MoveType / FromFloor / ToFloor / TaskType / UseFlag / Remark）
  - 初始資料（一廠專屬，僅 1F↔3F）：
    - `Transport`, `1F`, `3F`, `F13Test`, `Y`
    - `Transport`, `3F`, `1F`, `F31Test`, `Y`
    - `EmptyMove`, `1F`, `3F`, `F13Test`, `Y`
    - `EmptyMove`, `3F`, `1F`, `F31Test`, `Y`
  - 驗收：表結構與二廠一致，4 筆初始資料正確
  - **完成紀錄（2026-04-28）**：表已建立、4 筆初始資料全部 `UseFlag=Y` 已就位，DB 驗收 PASS

- [x] [P0] 建立 `Deploy_pFunction_TaskTypeRouteMenu.sql` 並執行
  - 新增 WebGui 後台選單：「跨樓層 TaskType 路由管理」
  - 驗收：後台可看到選單，點擊進入 CRUD 頁
  - **完成紀錄（2026-04-28）**：FunctionNo=506 已注入 pFunction，pGroup(GroupId=1).FunctionGroup 含 506。**注意**：實際後台選單頁要等 P2 `TaskTypeRouteController` 任務完成後才能點擊進入

---

### P0 — 設定檔與 Model 擴充

- [x] [P0] HikAGV.config 新增 `<AGVSettings>` 屬性
  - 路徑：`ACC/HikAGVWebAPI/HikAGV/Config/HikAGV.config`
  - 新增：
    ```xml
    CrossFloorShuttleId="3"
    IdleReturnTimeout="600"
    IdleReturnFloor="3F"
    MapCodeFloorMapping="AA:1F,DD:3F"  <!-- 待現場驗證 -->
    ```
  - 驗收：config 可被 ConfigAGVModel 解析，啟動 Log 看到正確值
  - **完成紀錄（2026-04-28）**：4 屬性已加入既有 `<AGVSettings>` 元素，保留舊有 `AGVMapCode` / `CrossFloorTaskTypeMap`（Dispatch.cs 仍引用，待後續任務再決定是否移除）。「啟動 Log 看到正確值」屬 P3 端對端驗證範圍，本任務僅驗證 schema/Build。

- [x] [P0] ConfigAGVModel 新增 4 個對應屬性
  - 路徑：`ACC/HikAGVWebAPI/HikAGV/Models/ConfigAGVModel.cs`
  - 屬性：`CrossFloorShuttleId` / `IdleReturnTimeout` / `IdleReturnFloor` / `MapCodeFloorMapping`
  - 驗收：屬性對應 config 欄位，型別正確，Build 通過
  - **完成紀錄（2026-04-28）**：4 屬性照搬二廠寫法，DefaultValue 改用一廠值（3 / 600 / 3F / AA:1F,DD:3F）。msbuild HikAGV.csproj 通過，0 error。

- [x] [P0] oShuttleModel 新增 `MapCode` / `UpdateTime` 屬性
  - 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/Models/oShuttleModel.cs`（或一廠對應位置）
  - 驗收：Model 屬性與 DB 欄位對應，SELECT / INSERT 能正確讀寫
  - **完成紀錄（2026-04-28）**：照搬二廠寫法，新增 `using System;` + `MapCode` (string) + `UpdateTime` (DateTime?) 屬性。msbuild HikAGVWebAPI.csproj 0 error。「SELECT/INSERT 讀寫」屬後續 SQLData 任務範圍。
  - **環境註記**：本次 build 前需從二廠拷貝 7 支 dll（System.Web.Http、MsSql.dll 等）+ 覆蓋 System.Net.Http.Formatting.dll 至 `bin/`。這是「fresh checkout 後 HikAGVWebAPI 缺 dll」的既有問題，非本任務引入；bin/ 在 .gitignore 內、不進版控。

---

### P1 — 核心邏輯移植（TDD）

- [ ] [P1] 🧪 移植 CooldownTracker
  - 來源：`SecondFactory/ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/CooldownTracker.cs`
  - 目的：`OneFactory/ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/CooldownTracker.cs`
  - 同時移植測試：`SecondFactory/.../HikAGVWebAPITests/App_Start/CrossFloorManagerCooldownTests.cs`
  - ├─ TDD 原因：純類別、有明確 I/O、共用元件
  - ├─ 測試分類：Happy Path / Edge Case / Time-based
  - └─ 預估測試案例數：4~6

  **測試案例矩陣：**

  | # | 案例名稱 | 分類 | 輸入 | 預期結果 | 優先序 |
  |---|---------|------|------|---------|--------|
  | 1 | 未呼叫過_RecordCompletion | Happy Path | new + IsInCooldown("P1") | false | P0 |
  | 2 | RecordCompletion後立即查同parent | Happy Path | RecordCompletion+IsInCooldown 同 key | true | P0 |
  | 3 | RecordCompletion後查不同parent | Happy Path | 不同 key | false | P0 |
  | 4 | 冷卻期過期自動失效 | Time-based | sleep > cooldown | false | P1 |
  | 5 | RecordCompletion 重複呼叫不會重置 | Edge | 同 key 兩次 | true（仍在冷卻）| P2 |
  | 6 | 多 parent 並存獨立計時 | Edge | P1+P2 同時 | 各自獨立 | P2 |

- [ ] [P1] 🧪 移植 CrossFloorManager 核心邏輯
  - 來源：`SecondFactory/.../App_Start/CrossFloorManager.cs`（477 行）
  - 目的：`OneFactory/.../App_Start/CrossFloorManager.cs`
  - 同時移植測試：`SecondFactory/.../CrossFloorManagerSelectNextMissionTests.cs`
  - ├─ TDD 原因：核心狀態機、多種例外路徑、共用元件
  - ├─ 測試分類：Happy Path / Edge Case / Error / Boundary
  - └─ 預估測試案例數：12~16
  - **重點**：`DecideNextCrossFloorAction` 是純函數，優先測試此函數覆蓋決策邏輯

  **測試案例矩陣（核心）：**

  | # | 案例名稱 | 分類 | 情境 | 預期結果 | 優先序 |
  |---|---------|------|------|---------|--------|
  | 1 | IDLE車於母樓層_不啟動計時 | Happy | currentFloor=3F | 重置計時 | P0 |
  | 2 | IDLE車離開母樓層_啟動計時 | Happy | currentFloor=1F | 啟動計時 | P0 |
  | 3 | 計時超時_產生IDLE_RETURN任務 | Happy | timer > 600s | Insert oMission | P0 |
  | 4 | 計時中收到新任務_重置計時 | Edge | OnNewTaskArrived | timer 歸零 | P0 |
  | 5 | 跨樓層待派且車在起點樓層_直接派 | Happy | SameFloor | 回傳父任務 | P0 |
  | 6 | 跨樓層待派且車不在起點樓層_預調度 | Happy | NeedDispatch | 產生 CROSS_FLOOR_DISPATCH | P0 |
  | 7 | 冷卻期命中_攔截重派 | Edge | CooldownHit | 回傳 CooldownHit + parent | P0 |
  | 8 | 預調度執行中_新任務排佇列 | Edge | 7號情境 | 等預調度完成 | P1 |
  | 9 | OnIdleReturnCompleted_清理狀態 | Happy | end 回呼 | _idleReturnDispatched=false | P1 |
  | 10 | OnCrossFloorDispatchCompleted_RecordCooldown | Happy | end 回呼 | 冷卻計時開始 | P1 |
  | 11 | 車輛離線(UpdateTime>60s)_跳過Tick | Error | 60秒未更新 | 不處理 | P1 |
  | 12 | MapCode 找不到對應樓層_視為未知 | Error | 未知前綴 | 跳過、寫 Log | P1 |
  | 13 | 同樓層任務優先於跨樓層任務 | Boundary | 混合任務 | 派同樓層 | P2 |
  | 14 | DispatchIdleReturn DB 失敗_保留計時 | Error | Insert 例外 | Log Warning | P2 |
  | 15 | DispatchCrossFloor DB 失敗_保留狀態 | Error | Insert 例外 | Log Warning | P2 |
  | 16 | OnTaskCancelled_清理對應狀態 | Edge | cancel 回呼 | 重置狀態 | P2 |

- [ ] [P1] 🧪 SQLData 新增 oTaskTypeRoute Repository
  - 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/...SQLData.cs`（一廠對應位置）
  - 新增方法：
    - `Select_oTaskTypeRoute(string moveType, string fromFloor, string toFloor)` — 查單筆
    - `GetTaskType(string moveType, string fromFloor, string toFloor)` — 回傳 TaskType 字串
  - ├─ TDD 原因：DB 存取邏輯、有明確 I/O
  - ├─ 測試分類：Happy Path / Error
  - └─ 預估測試案例數：3~4

  **測試案例矩陣：**

  | # | 案例名稱 | 分類 | 輸入 | 預期結果 | 優先序 |
  |---|---------|------|------|---------|--------|
  | 1 | 1F到3F Transport路由存在_回傳TaskType | Happy | (Transport,1F,3F) | "F13Test" | P0 |
  | 2 | 路由不存在_回傳null | Error | (Transport,2F,4F) | null + Log Warning | P0 |
  | 3 | UseFlag=N_視為不存在 | Edge | UseFlag=N 的路由 | null | P0 |
  | 4 | 大小寫敏感性確認 | Boundary | "transport" vs "Transport" | 依設定 | P1 |

---

### P2 — 整合掛接（無 TDD，已被 P1 測試覆蓋）

- [ ] [P2] Dispatch.cs 掛接 CrossFloor
  - 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/App_Start/Dispatch.cs`
  - 修改：
    1. 新增 `public static CrossFloorManager CrossFloor { get; set; }`
    2. 在 `InitialData()` 內 new CrossFloorManager 並指派
    3. 在 `Execute()` 主循環中 `AGVSchedulingTask()` 後新增 `CrossFloor?.Tick();`
  - 驗收：啟動後 Log 看到 CrossFloor 初始化訊息，Tick 每秒執行

- [ ] [P2] CallBackAPI.cs 補 CrossFloor 完成回呼
  - 路徑：`ACC/HikAGVWebAPI/HikAGVWebAPI/API/CallBackAPI.cs`
  - 修改：
    - `case CallBackMethod.end`：新增 `Dispatch.CrossFloor?.OnIdleReturnCompleted(oMission)` 與 `OnCrossFloorDispatchCompleted(oMission)`（依 TaskSource 分流）
    - `case CallBackMethod.cancel`：新增 `Dispatch.CrossFloor?.OnTaskCancelled(...)`
  - 驗收：跨樓層任務完成 Callback 後，CrossFloorManager 內部狀態正確重置

---

### P2 — WebGui 後台

- [ ] [P2] WebGui 新增 TaskTypeRouteController CRUD
  - 路徑：`WebGui/SCP/Controllers/TaskTypeRouteController.cs`
  - 對應 View：`WebGui/SCP/Views/TaskTypeRoute/Index.cshtml`
  - 功能：列表 / 新增 / 編輯 / 啟停用 / 刪除 oTaskTypeRoute
  - 驗收：後台可正常 CRUD，UseFlag 切換 Y/N 即時生效

---

### P3 — 端對端驗證與文件

- [ ] [P3] 端對端整合測試
  - 情境 1：跨樓層車（ID=3）開到 1F 閒置 600 秒 → 觀察自動產生 IDLE_RETURN 任務 → 車輛回到 3F
  - 情境 2：手動下達 1F→3F 任務但車在 3F 閒置 → 觀察產生 CROSS_FLOOR_DISPATCH 任務 → 預調度完成後派發原始任務
  - 情境 3：預調度完成 30 秒內再下達同 parent 任務 → 觀察冷卻期攔截
  - 情境 4：一廠既有功能（Port、路線權限、空平板派送）回歸測試 → 全部正常
  - 驗收：4 個情境全部通過

- [ ] [P3] 撰寫一廠功能說明書
  - 路徑：`Doc/功能說明書_一廠AGV跨樓層自動化機制.md`
  - 內容：模仿二廠 `Doc/功能說明書_二廠三項自動化功能.md` 結構（去除空平板派送章節）
  - 驗收：包含「自動回歸」與「預派調度」兩章節，含情境表、配置說明、部署清單

---

## 進度追蹤

- 總子任務數：14
- 🧪 TDD 任務：3（CooldownTracker、CrossFloorManager、oTaskTypeRoute Repository）
- 一般任務：11
- 預估測試案例數：22~26（P0：14，P1：6~7，P2：3~5）

## Commit 策略

依 `plan.md` 末段「Commit 策略」表，13 個 commit 依序提交。

## 風險清單

| 風險 | 緩解措施 |
|------|---------|
| 一廠 SQLData 介面與二廠不一致 | 移植前先 diff `SQLData.cs`，列出差異，CrossFloorManager 內呼叫處逐一適配 |
| 一廠 namespace / using 與二廠不同 | 複製檔案後執行全文 replace，Build 失敗時逐一修正 |
| MapCode 對應樓層需現場驗證 | HikAGV.config 先寫推測值，部署前向客戶確認實際對應 |
| 一廠 WebGui 版本（MVC / Razor）與二廠差異 | 複製 PortBindingController 模式作為樣板，比對版本後調整 |
| oMission.TaskSource 既有資料無 IDLE_RETURN/CROSS_FLOOR_DISPATCH 值 | 不需 backfill，新任務寫入即可，UI 側對未知值顯示為「手動任務」 |
