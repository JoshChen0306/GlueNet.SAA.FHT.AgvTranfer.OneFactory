# 多樓層地圖座標轉換系統實作計畫

## 背景
目前 `ShowMap` 功能使用固定的座標轉換參數（針對 1F），無法正確顯示其他樓層的庫位和 AGV。需要重構為依據樓層 (area) 動態讀取對應的座標參數。

---

## 現有演算法分析

### 座標轉換公式（線性插值）

```
UI座標% = minPercent + ((RCS座標 - minRCS) / (maxRCS - minRCS)) × (maxPercent - minPercent)
```

### 1F 現有配置
| 參數 | 值 |
|------|---------|
| RCS X 範圍 | 200000 ~ 237582 |
| RCS Y 範圍 | 200000 ~ 218228 |
| UI X 範圍 | 17% ~ 91.5% |
| UI Y 範圍 | 14% ~ 89% |

---

## 2F 參數計算過程

### 已知資料

**雷雕備貨區 (M區)** - 應顯示在**左邊中間**
| 站點 | RCS X | RCS Y |
|------|-------|-------|
| M1 | 223212 | 260341 |
| M2 | 223212 | 259495 |
| M3 | 223202 | 258654 |
| M4 | 223204 | 257811 |

**清洗區 (S區)** - 應顯示在**右邊下方**
| 站點 | RCS X | RCS Y |
|------|-------|-------|
| S1 | 245592 | 253166 |
| S2 | 244747 | 253188 |

### 座標方向分析
- X 座標：M區 (~223,200) < S區 (~245,000) → **X 越大越右** ✅
- Y 座標：M區 (~258,000-260,000) > S區 (~253,000) → **Y 越大越上** ✅

### 計算過程

**參考點設定：**
- M區中心: rcsX_M = 223207, 目標 targetX_M ≈ 18%
- S區中心: rcsX_S = 245170, 目標 targetX_S ≈ 72%

**X 軸計算：**
```
斜率 = (72 - 18) / (245170 - 223207) = 54 / 21963 ≈ 0.002459
minX = 223207 - (18 - 17) / 0.002459 ≈ 222800
maxX = 222800 + (91.5 - 17) / 0.002459 ≈ 253100
```

**Y 軸計算：**
```
斜率 = (50 - 25) / (260341 - 253166) = 25 / 7175 ≈ 0.003484
minY = 253166 - (25 - 14) / 0.003484 ≈ 250000
maxY = 250000 + (89 - 14) / 0.003484 ≈ 271500
```

### 2F 計算結果
| 參數 | 值 |
|------|---------|
| minX | 222800 |
| maxX | 253700 |
| minY | 250000 |
| maxY | 271500 |

---

## 微調建議

若實際顯示有偏差，可依以下原則調整：

| 現象 | 調整方式 |
|------|---------|
| 站點偏左 | 增加 minX 或減少 maxX |
| 站點偏右 | 減少 minX 或增加 maxX |
| 站點偏上 | 增加 minY 或減少 maxY |
| 站點偏下 | 減少 minY 或增加 maxY |

---

## 程式修改

### 1. appsettings.json

將 `AgvSetting` 從單一配置重構為多樓層配置：

```json
"AgvSetting": {
  "FHT2-1F": {
    "minPercentX": "17",
    "maxPercentX": "91.5",
    "minPercentY": "14",
    "maxPercentY": "89",
    "minX": "200000",
    "maxX": "237582",
    "minY": "200000",
    "maxY": "218228"
  },
  "FHT2-2F": {
    "minPercentX": "17",
    "maxPercentX": "91.5",
    "minPercentY": "14",
    "maxPercentY": "89",
    "minX": "222800",
    "maxX": "253700",
    "minY": "250000",
    "maxY": "271500"
  },
  "FHT2-3F": { ... },
  "FHT2-4F": { ... }
}
```

### 2. CommonController.cs

`ConvertX` 和 `ConvertY` 方法增加 `area` 參數：

```csharp
private string ConvertX(string posX, string area)
{
    var setting = _configuration.GetSection($"AgvSetting:{area}");
    double minPercentX = Convert.ToDouble(setting["minPercentX"]);
    // ... 其餘邏輯不變
}
```

---

## 驗證計畫

1. **1F 驗證**: 確認站點和 AGV 顯示位置與修改前相同
2. **2F 驗證**: 確認雷雕區顯示在左邊中間，清洗區顯示在右下方
3. **參數微調**: 根據實際顯示效果調整參數
