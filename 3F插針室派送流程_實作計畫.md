# 3F 插針室派送流程變更 實作計畫

## 目標

變更 3F 插針室 (J 區) 派送流程，改為「先建立物料，再選擇起點」模式，與 2F 雷雕區派送邏輯一致。

## 現況分析

### 現行流程
```
1. 選擇樓層 = 3F
2. 選擇派送區域 = J（插針室）
3. 選擇起點站點（如 J1）
4. 終點自動帶出 EE 區（1F電梯暫存區）空架
5. 輸入工單 → 確認派車
```

### 目標流程（參考 2F 雷雕區）
```
1. 點擊地圖上的 J 區站點（如 J1）
2. 彈出物料登記 Modal → 輸入工單和貨架
3. 確認儲存 → 站點狀態改為「料盤」
4. 選擇樓層 = 3F
5. 選擇派送區域 = J（插針室）
6. 選擇起點 → 只顯示已建立物料的站點
7. 終點自動帶出 EE 區（1F電梯暫存區）空架
8. 確認派車
```

---

## 站點對應表

| 區域 | 系統代號 | 站點範例 | 樓層 | 用途 |
|------|----------|----------|------|------|
| 插針室 | J | J1~Jn | 3F | 插針作業區 |
| 電梯暫存區 | EE | EE1~EEn | 1F | 電梯前暫存區 |

---

## 派送路線總覽

| 路線 | 起點 | 終點 | 功能 |
|------|------|------|------|
| 1 | J (3F 插針室) | EE (1F 電梯暫存區) | 物料派送 |
| 2 | EE (1F 電梯暫存區) | J (3F 插針室) | **Release 回送空板** ← 新增 |

---

## EE 區 Release 功能

### 操作流程

1. 貨物送達 EE 區後，人員取走物料
2. 人員在地圖上點擊 EE 區站點，彈出操作視窗
3. 點擊「標記為空板」
   - `HaveFlag` 改為 `1`（無料有板）
   - `WorkOrder` 清除為 `NULL`
4. 點擊「Release」
   - 系統自動尋找 **J 區空位**（HaveFlag=0）
   - 找到空位 → 派車回送
   - 無空位 → 彈出提示「目前沒有可放置的貨架」

### UI 設計：EE 區站點操作彈窗

**情況 1：料盤（HaveFlag = 3）**
```
┌────────────────────────────────────────────┐
│  📍 EE1 站點操作                           │
├────────────────────────────────────────────┤
│  目前狀態：料盤                             │
│  工單：FHT^N01^238090671^...               │
│  貨架：RACK001                              │
├────────────────────────────────────────────┤
│  [📦 標記空板]                  [關閉]      │
└────────────────────────────────────────────┘
```

**情況 2：空板（HaveFlag = 1）**
```
┌────────────────────────────────────────────┐
│  📍 EE1 站點操作                           │
├────────────────────────────────────────────┤
│  目前狀態：空板                             │
│  工單：無                                   │
│  貨架：RACK001                              │
├────────────────────────────────────────────┤
│  [🚚 Release 回送]              [關閉]      │
└────────────────────────────────────────────┘
```

### Release 判斷邏輯

```javascript
Release(stationNo) {
    1. 檢查站點狀態是否為 HaveFlag=1（空板）
       → 若不是，提示「請先標記為空板」
    
    2. 尋找 J 區（3F 插針室）可放置位置：
       WHERE Block='J' AND HaveFlag=0 AND BgnToEnd IS NULL AND UseFlag='Y'
       ORDER BY Priority DESC
    
    3. 結果處理：
       → 找到空位：建立派送任務 (EE → J)
       → 無空位：彈出提示「目前沒有可放置的貨架」
}
```

---

## Proposed Changes

### 前端 (Dispatch.js)

#### [MODIFY] [Dispatch.js](file:///d:/專案管理/Custom/GlueNet.SAA.FHT.AgvTranfer.SecondFactory/WebGui/SCP/wwwroot/js/Dispatch.js)

**1. 新增 J 區和 EE 區點擊支援**

目前 `validAreas` 只包含 `['M', 'T', 'O', 'P', 'S', 'N']`，需新增 `'J'` 和 `'E'`（EE 區）：

```diff
// 處理 M/T/J 區（物料登記）和 O/P/S/N/EE 區（標記空板/Release）
- var validAreas = ['M', 'T', 'O', 'P', 'S', 'N'];
+ var validAreas = ['M', 'T', 'O', 'P', 'S', 'N', 'J', 'E'];  // E 代表 EE 區

**2. 修改 `openStationLotModal` 函數**

讓 J 區支援物料登記，EE 區支援 Release 操作：

```diff
// 判斷區域類型：M/T/J 區為物料登記區，O/P/S/N/EE 區為 Release 操作區
- var releaseAreas = ['O', 'P', 'S', 'N'];
+ var releaseAreas = ['O', 'P', 'S', 'N', 'E'];  // E 代表 EE 區

if (isReleaseArea) {
    // O/P/S/N/EE 區 - Release 操作邏輯...
} else {
-   // M/T 區 - 物料登記操作
+   // M/T/J 區 - 物料登記操作
    // ...
}
```

**3. 修改 V Cut checkbox 顯示邏輯**

J 區不需要 V Cut checkbox：

```diff
// 根據站點區域控制 V Cut checkbox 顯示
if (stationArea === "T") {
    $("#vcutCheckboxRow").hide();
} else if (stationArea === "M") {
    $("#vcutCheckboxRow").show();
+} else if (stationArea === "J") {
+   $("#vcutCheckboxRow").hide();  // 插針室不需要 V Cut 標記
} else {
    $("#vcutCheckboxRow").hide();
}
```

**4. 修改樓層選擇邏輯**

選擇 3F 時，顯示掃描機台按鈕並初始化相關欄位（類似 2F）：

```javascript
// 3F 樓層特別處理
if (selectedFloor === "3F") {
    // 預設選擇 J（插針室）
    if ($("#Area option[value='J']").length > 0) {
        $("#Area").val("J").trigger("change");
    }
    // 隱藏 Rack 碼和工單欄位（已在物料登記時輸入）
    $("#rackIdRow").hide();
    $("#workOrderRow").hide();
}
```

**5. 修改起點選項過濾**

J 區起點只顯示已建立物料的站點（`HaveFlag = 3`）：

```javascript
function filterBeginStationOptions(selectedValue) {
    // ... existing code ...
    
    // MT 區或 J 區特別處理：只顯示有料站點
    if (selectedValue === "MT" || selectedValue === "J") {
        // 過濾只顯示 HaveFlag=3 的站點
        // ...
    }
}
```

**6. 修改終點自動選擇**

```javascript
case "J":
    autoSelectEndStation("EE", "0", "N");  // J區 → EE區（1F 電梯暫存區）
    break;
```

**7. J 區工單自動帶入**

J 區派送時，工單會自動從站點資料帶入（因為已在物料登記時輸入），不需手動輸入：

```javascript
// J 區（3F 插針室）特別處理：工單已在物料登記時輸入，自動帶入
else if (area === "J" && beginStation) {
    var beginStationCache = stationCache[beginStation];
    if (beginStationCache && beginStationCache.workOrder) {
        $("#WorkOrder").val(beginStationCache.workOrder);
    }
}
```

**8. 防止重複派工**

派工成功後清除站點快取，下次選擇起點時會重新從 API 載入資料（API 會自動排除已有待處理任務的站點）：

```javascript
success: function (response) {
    form[0].reset();
    // 清除站點快取，防止重複派工
    isCacheLoaded = false;
    stationCache = {};
}
```

**9. 終點站驗證**

當沒有可用終點時（所有空位都已被指派），提示使用者：

```javascript
// 驗證：終點站必選
if (!endStation || endStation === "" || endStation === "選擇站點") {
    alert('沒有可用的派送終點，請確認目標區域有空位');
    allValid = false;
}
```

---

### 後端 (DispatchController.cs)

#### [MODIFY] [DispatchController.cs](file:///d:/專案管理/Custom/GlueNet.SAA.FHT.AgvTranfer.SecondFactory/WebGui/SCP/Controllers/DispatchController.cs)

**1. RegisterLot 方法支援 J 區**

目前 `RegisterLot` 已支援 M/T 區，J 區新增以下驗證：
- **RackId 必填**：3F (J 區) 建立物料時強制輸入貨架條碼

```csharp
// 驗證：J 區（3F 插針室）RackId 必填
var stationArea = stationNo.Substring(0, 1).ToUpper();
if (stationArea == "J" && string.IsNullOrEmpty(rackId))
{
    return BadRequest(new { message = "請輸入貨架條碼" });
}
```

**2. ClearLot 方法支援 J 區**

同上，確認通用邏輯可支援 J 區。

**3. GetAllStations API 防止重複派工**

修改 API 同時排除待處理任務的起點站和終點站：

```csharp
// 取得已有待處理任務的終點站，避免重複指派
var pendingEndFromONeed = _DBContext.oNeed
    .Where(n => n.AssignFlag == null || n.AssignFlag == "")
    .Select(n => n.EndStation)
    .ToList();

// 合併所有不可選的站點（起點 + 終點）
var allExcludedStations = allPendingBeginStations
    .Concat(allPendingEndStations)
    .Distinct()
    .ToList();

var stations = _DBContext.oPort
    .Where(p => p.UseFlag == "Y" && 
                !allExcludedStations.Contains(p.StationNo));
```

---

### 後端 (cPair.cs)

#### [CONFIRM] [cPair.cs](file:///d:/專案管理/Custom/GlueNet.SAA.FHT.AgvTranfer.SecondFactory/Dispatch/svrPair/cPair.cs)

目前第 172 行已包含 `case "J":`，無需修改：

```csharp
case "J":   // <--- ★★★ 3F 插針室 ★★★
```

---

## UI 設計

### 物料登記 Modal（J 區）

```
┌────────────────────────────────────────────┐
│  📍 J1 站點操作                            │
├────────────────────────────────────────────┤
│  目前狀態：空架                             │
│  工單：無                                   │
│  貨架：無                                   │
├────────────────────────────────────────────┤
│  [📋 物料登記]                  [關閉]      │
└────────────────────────────────────────────┘
```

### 物料登記表單（J 區專用，無 V Cut checkbox）

```
┌────────────────────────────────────────────┐
│  📋 物料登記                               │
├────────────────────────────────────────────┤
│  工單條碼：[                    ] [掃描]   │
│  貨架條碼：[                    ] [掃描]   │
├────────────────────────────────────────────┤
│        [取消]     [✅ 確認儲存]             │
└────────────────────────────────────────────┘
```

### 派送起點選擇（顯示已有料的站點）

```
派送起點：
┌─────────────────────────────────────────────┐
│  [下拉選擇]                              ▼  │
├─────────────────────────────────────────────┤
│  J1 - FHT^N01^238090671^DMT6CVJ1536D        │  ← 只顯示有料站點
│  J3 - FHT^N02^238090672^ABC123456           │
└─────────────────────────────────────────────┘
```

---

## 派送流程總覽

| 步驟 | 使用者操作 | 系統行為 |
|------|----------|----------|
| 1 | 開啟 Dispatch 頁面，選擇 3F | 地圖切換為 3F |
| 2 | 點擊地圖上的 J 區站點（如 J1） | 彈出站點操作 Modal |
| 3 | 點擊「物料登記」 | 顯示輸入表單 |
| 4 | 輸入工單和貨架 → 確認儲存 | 站點狀態更新為「料盤」 |
| 5 | 選擇派送區域 = J（插針室） | - |
| 6 | 選擇派送起點 | 只顯示已建立物料的 J 區站點 |
| 7 | - | 終點自動帶出 EE 區空架 |
| 8 | 按下「確認派車」 | 建立派送任務 |

---

## 資料流

| 時間點 | 站點 | HaveFlag | WorkOrder | RackId |
|--------|------|----------|-----------|--------|
| 初始狀態 | J1 | 0 (空架) | NULL | NULL |
| 物料登記後 | J1 | 3 (料盤) | FHT^N01^... | RACK001 |
| 派送中 | J1→EE1 | - | - | - |
| 到達 EE1 | J1 | 0 (空架) | NULL | NULL |
| 到達 EE1 | EE1 | 3 (料盤) | FHT^N01^... | RACK001 |

---

## 需要開發的功能模組

### J 區（3F 插針室）功能

| 模組 | 前端修改 | 後端修改 | 說明 |
|------|----------|----------|------|
| **J 區點擊支援** | ✅ 新增 'J' 到 validAreas | 無需修改 | 讓 J 區站點可點擊開啟 Modal |
| **J 區物料登記** | ✅ 修改 openStationLotModal | 確認 RegisterLot 通用 | 支援 J 區物料登記 |
| **隱藏 V Cut checkbox** | ✅ J 區隱藏 V Cut 選項 | 無需修改 | 插針室不需要 V Cut 標記 |
| **3F 樓層初始化** | ✅ 選擇 3F 時隱藏工單欄位 | 無需修改 | 模仿 2F 的行為 |
| **起點過濾** | ✅ J 區只顯示有料站點 | 無需修改 | 只能選擇已建立物料的站點 |
| **終點自動選擇** | 無需修改 | 無需修改 | 現有邏輯已支援 J→EE |

### EE 區（1F 電梯暫存區）功能

| 模組 | 前端修改 | 後端修改 | 說明 |
|------|----------|----------|------|
| **EE 區點擊支援** | ✅ 新增 'E' 到 validAreas 和 releaseAreas | 無需修改 | 讓 EE 區站點可點擊開啟 Modal |
| **EE 區標記空板** | 無需修改 | 確認 MarkEmptyTray 通用 | 複用現有「標記空板」功能 |
| **EE 區 Release** | ✅ 修改 Release 回送邏輯 | ✅ 新增 EE→J 回送判斷 | EE 區 Release 回送到 J 區 |

---

## Verification Plan

### 手動測試步驟

#### 測試 1：物料登記功能

1. 開啟 Dispatch 頁面
2. 選擇樓層 = **3F**
3. 確認地圖切換為 3F 地圖
4. 點擊地圖上的 J 區站點（如 J1）
5. 確認彈出站點操作 Modal
6. 點擊「📋 物料登記」按鈕
7. 輸入工單條碼和貨架條碼
8. 確認**沒有** V Cut checkbox
9. 點擊「確認儲存」
10. 驗證站點狀態變為「料盤」

#### 測試 2：派送起點過濾

1. 延續測試 1（J1 已有物料）
2. 選擇派送區域 = J（插針室）
3. 點擊派送起點下拉選單
4. 驗證：
   - ✅ J1 顯示在選項中（有料）
   - ❌ 其他空架 J 區站點不顯示

#### 測試 3：終點自動選擇

1. 延續測試 2
2. 選擇起點 = J1
3. 驗證終點自動帶出 EE 區空架（如 EE1）

#### 測試 4：完整派送流程

1. 延續測試 3
2. 點擊「確認派車」
3. 驗證 oNeed 表中新增一筆記錄：
   ```sql
   SELECT * FROM oNeed WHERE ObjStation = 'J1'
   ```

#### 測試 5：物料修改與清除

1. 點擊已有物料的 J 區站點
2. 點擊「✏️ 物料修改」→ 確認可修改工單/貨架
3. 點擊「🗑️ 清除物料」→ 確認二次確認後清除成功

#### 測試 6：EE 區標記空板

1. 選擇樓層 = **1F**
2. 點擊地圖上的 EE 區站點（假設 EE1 已有物料）
3. 確認彈出站點操作 Modal，顯示「📦 標記空板」按鈕
4. 點擊「標記空板」
5. 驗證站點狀態變為「空板」(HaveFlag=1)，工單清除

#### 測試 7：EE 區 Release 回送

1. 延續測試 6（EE1 已標記為空板）
2. 點擊 EE1 站點，確認顯示「🚚 Release 回送」按鈕
3. 點擊「Release 回送」
4. 驗證：
   - 若 J 區有空位 → 建立派送任務 (EE1 → J區空位)
   - 若 J 區滿 → 彈出提示「目前沒有可放置的貨架」
5. 驗證 oNeed 表中新增一筆記錄：
   ```sql
   SELECT * FROM oNeed WHERE ObjStation = 'EE1' AND EndStation LIKE 'J%'
   ```

---

## 設計決策記錄

| 項目 | 決定 |
|------|------|
| V Cut 標記 | 3F 插針室**不需要** V Cut 標記功能 |
| J 區終點選擇 | J 區終點固定為 **EE 區（1F 電梯暫存區）** |
| J 區起點過濾 | 派送起點「只顯示已建立物料的 J 區站點」 |
| 工單欄位 | 3F 選擇後隱藏工單欄位（已在物料登記時輸入） |
| **EE 區 Release** | EE 區 Release 回送至 **J 區（3F 插針室）** ← 新增 |
