# 2F 派送路線實作計畫

## 需求說明

| 路線 | 起點 | 終點選項 | 供單號 |
|------|------|----------|--------|
| 1 | M (雷雕備貨區) | O (左上料區), P (右上料區), T (V cut區) | **必填** |
| 2 | T (V cut區) | O (左上料區), P (右上料區) | **必填** |
| 3 | Q (出料區) | S (清洗區) | 不需要 |
| 4 | R (廢料區) | N (廢料回收區) | 不需要 |

> [!IMPORTANT]
> 從 **M (雷雕區)** 或 **T (V Cut區)** 出發時，必須輸入供單號才能派發任務。

---

## Proposed Changes

### 前端

#### [MODIFY] [Dispatch.js](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/WebGui/SCP/wwwroot/js/Dispatch.js)

**1. BeginStation change 事件 (switch area)**

```javascript
case "M":
    // 雷雕區可選: O(左上料), P(右上料), T(V cut) - 需要供單號
    $("#EndStation").prop("disabled", false);  // 使用者自選終點
    $("#WorkOrder").prop("disabled", false);   // 啟用供單號輸入
    $("#WorkOrder").prop("required", true);    // 設為必填
    break;
case "T":
    // V cut區可選: O(左上料), P(右上料) - 需要供單號
    $("#EndStation").prop("disabled", false);
    $("#WorkOrder").prop("disabled", false);   // 啟用供單號輸入
    $("#WorkOrder").prop("required", true);    // 設為必填
    break;
case "Q":
    autoSelectEndStation("S", "0", "N");  // 出料區 → 清洗區
    $("#WorkOrder").prop("disabled", true);
    $("#WorkOrder").prop("required", false);
    break;
case "R":
    autoSelectEndStation("N", "0", "N");  // 廢料區 → 廢料回收區
    $("#WorkOrder").prop("disabled", true);
    $("#WorkOrder").prop("required", false);
    break;
```

**2. EndStation focus 事件 (filterEndStationOptions)**

```javascript
case "M":
    filterEndStationOptions("O", "0");  // 顯示 O, P, T 區空架
    filterEndStationOptions("P", "0");
    filterEndStationOptions("T", "0");
    break;
case "T":
    filterEndStationOptions("O", "0");  // 顯示 O, P 區空架
    filterEndStationOptions("P", "0");
    break;
case "Q":
    filterEndStationOptions("S", "0");
    break;
case "R":
    filterEndStationOptions("N", "0");
    break;
```

**3. 派發前驗證 (在派發按鈕 click 事件中)**

```javascript
// 檢查 M 或 T 區是否有填寫供單號
var beginArea = $("#BeginStation option:selected").data("area");
if ((beginArea === "M" || beginArea === "T") && !$("#WorkOrder").val().trim()) {
    alert("從雷雕區或V Cut區出發，必須輸入供單號！");
    return false;
}
```

---

### 後端

#### [MODIFY] [cPair.cs](file:///d:/00.Program/汛德機械/高技自動搬運系統/客戶提供資料/SourceCode/Git/Dispatch/svrPair/cPair.cs)

在 `GenerateoRequireByoNeed` 方法的 switch 新增：

```csharp
case "M":   // 雷雕區 → O/P/T
case "T":   // V cut區 → O/P
case "Q":   // 出料區 → 清洗區
case "R":   // 廢料區 → 廢料回收區
    WriteLog("05.處理平板配對");
    ProcessoNeedToRequire(dr["ObjStation"].ToString().Substring(0, 1), dr);
    break;
```

---

## Verification Plan

1. 重新編譯 Dispatch 專案和 WebGui
2. 測試各路線：
   - M → O/P/T (使用者可選)
   - T → O/P (使用者可選)
   - Q → S (自動選擇)
   - R → N (自動選擇)
